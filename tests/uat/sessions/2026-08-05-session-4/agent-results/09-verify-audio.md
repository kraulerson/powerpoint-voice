# Independent skeptic review of UAT-4

**My harness** (built from scratch, nothing of the first tester's reused): `/private/tmp/claude-501/-Users-karl-Documents-Claude-Projects-powerpoint-voice/05a4d2ab-f2bc-4839-9c01-2ee22b3c3560/scratchpad/uat4/skeptic/` — `rel/` (own Release), `ubsan/` (own `-fsanitize=address,undefined -fno-sanitize-recover=undefined`), `fix/` (patched copy in `tree/`), own deck builder `mk.py` + 40 own fixtures in `d/`, own `px.py` pixel counter, own `geom_probe.cpp` / `prevent_probe.cpp`. Every cited `file:line` was checked against HEAD and all match. Confidential deck: only `<p:ph>` attribute values, rel Targets, blip nesting depth and loader-emitted EMU geometry were read; never opened for content, never rendered, no PNG of it read.

| # | Verdict | Agreed SEV | My own evidence | Smallest fix |
|---|---|---|---|---|
| **1** placeholderKey namespaced by holder | **CONFIRMED** | **SEV-1** | Own A/B: `rel/render_preview d/f1a_layout_sp.pptx` → `nonwhite=0`; `d/f1b_layout_pic.pptx` (identical but layout holder is `<p:pic>`) → `(255,0,0)×8008`. Both "0 warning(s)". On the real deck my `geom_probe` prints slide 1 el#2 `Image x=0 y=0 cx=0 cy=0 imgBytes=198219` — decoded ≠ null so the placeholder branch is skipped, `r.width()>=1` false, nothing drawn, no warning. `git show 460f390` confirms the pre-`571ccce` key was `type:idx`. | Drop `holderKind` from the key (match on `type:idx`, ECMA matches on `idx`) and make `parseLayoutPlaceholders` **insert-first-wins** (`&& !map.contains(key)`) so BUG-58's collision still can't let a later shape overwrite. I applied exactly this in `skeptic/tree`: real-deck slide 1 becomes `x=8017727 y=-1 cx=4282068 cy=6858001`, and the suite is 202/202 (the one intermittent `test_pre_render_worker.cpp:223 REQUIRE(fin.wait(5000))` failure reproduces on **unpatched** HEAD too — pre-existing flake, not the patch). |
| **2** `sourceRect()` int overflow at :217/:218 | CONFIRMED, **OVERSTATED** | SEV-3 | `ubsan/render_preview d/g2a_int_min_and_neg1.pptx` → `slide_renderer.cpp:217:66: runtime error: signed integer overflow: -2147483648 + -1`; `g2b` (`-2e9 + -2e9`) same; `g2c` hits `:218`. Release: `nonwhite=0`, `0 warning(s)`; `g2d` (single `l` only, no overflow) draws fine. | Sum in `qint64` on 217/218 as 210-211 already do, or reject `hSum <= -100000` in the same guard. **Severity**: identical observable impact to F6 (one picture silently gone from an out-of-schema attribute), which the same report rated SEV-3; needs `\|value\| ≈ 2³¹`, which PowerPoint cannot emit. Not "major". |
| **3** PREVENT counts runs, not characters; CONTAIN unwired | **CONFIRMED** | **SEV-2** | Own 133 786-byte deck (1 shape / 2 000 paras × 40 000 chars): `prevent_probe d/k_big.pptx` → `shapes=1 textRuns=2000 chars=80000000 PREVENT-gate: PASSES -> QPainter` / `slide 1 render: 115.20 s` (worse than the 52 s reported). Load is 0.06 s. `grep -rn "::progress" src/` → **no hits**; `app_shell.cpp:219-222` connects only `started`/`slideReady`/`finished`; `setCaps` is never called in `src/`. Multiplier verified: a **7 869-byte** deck with 300 `<p:sldId>` all targeting `slides/slide1.xml` loads as `slides=300`. Bible line 104 promises "complexity caps **+ hard deadline** → placeholder" — the deadline does not exist, so this is a gap against a ratified mitigation, and the Bible names the .pptx as the primary attack surface. | Add `chars` to `SlideComplexity`/`exceedsCaps` (cap ~2e6), and/or connect `progress` and give `renderOne` a per-slide deadline that falls back to `placeholderFn_`. |
| **4** blip found by unscoped subtree search | CONFIRMED, **OVERSTATED** | SEV-3 | `d/g4a_line_fill_blip` (no `<a:blip>` in `<p:blipFill>`; one in `<p:spPr><a:ln><a:blipFill>`) and `d/g4b_hlink_blip_shadow` (`<p:blipFill><a:blip r:embed="rId1"/>` present, decoy in `<p:cNvPr><a:hlinkClick>`) both render `(0,255,0)×8008` — the planted rId2, never rId1. | `pugi::xml_node blip = childLocal(childLocal(node,"blipFill"), "blip");` — safe: all **51** `<p:pic>` in the real deck have `<a:blip>` as a *direct* child of `<p:blipFill>`. **Severity**: both triggers are out-of-schema (`a:ln` takes no `blipFill`; `a:hlinkClick` takes no `a:blip`), and an attacker who can rewrite the XML can rewrite `r:embed` anyway — the only gain is stealth from XML review. Not SEV-2. |
| **5** layout/master picked from a QHash by Target substring | CONFIRMED, **OVERSTATED** | SEV-3 | 14 launches each of my own decks: `g5a_two_layouts` 7 red / 7 blue; `g5b_substring_image` (an *image* rel targeting `../media/slideLayout-logo.png`) 7 red / 7 green — layout bg **and** all placeholder geometry lost half the time, silently; `g5d_master_substring2` 8 green / 6 white. `parseRels` (deck_loader.cpp) discards `Type` entirely, so a Target substring is the only thing left to match on. | Keep the rel `Type` URI in `parseRels` and match `.../slideLayout` / `.../slideMaster` on Type, iterating in document order. **Severity**: does not fire on the reference deck — I checked every rels part: exactly one `slideLayout` Target per slide and one `slideMaster` per layout (the only multi-match is `slideMaster1.xml.rels`, which this code never iterates). Needs ≥2 matches, i.e. a hostile or perversely-named deck. |
| **6** negative `alphaModFix amt` → opacity 0 | **CONFIRMED** | **SEV-3** | `amt="-2147483648"`, `"-1"`, `"-0.000001%"` → `nonwhite=0`, `0 warning(s)`. `amt="50%"` → `(255,129,129)`; `amt="1e30%"` → red + `warning: unreadable opacity; drawing it opaque`. | At :700-712 treat out-of-range like unparseable: `if (!parsePercentAttr(...) \|\| parsed < 0 \|\| parsed > 100000)` → 100000 + the existing warning. |
| **7** `static_cast<int>(NaN)` in `clampFontPx` | **CONFIRMED** | **SEV-3** | `ubsan/render_preview d/g7a_sz_nan.pptx` → `slide_renderer.cpp:31:29: runtime error: nan is outside the range of representable values of type 'int'`; same for `sz="NaN"`. `±inf` do **not** trip it (they clamp). | `if (!std::isfinite(px)) px = 1.0;` before the clamp. Note for triage: on the shipping arm64 target the cast yields 0, `QFont::setPixelSize(0)` is ignored and the run renders at the default size (373 non-white px vs 185 normal) — real UB, **zero** user-visible loss here. |
| **8** no `else` on the destination-frame guard | **CONFIRMED** | **SEV-3** | `cx=cy=0`, `cx=-4e6`, `cx="99999999999999999999"`, `cx=cy=1` → all `nonwhite=0`, `0 warning(s)`; the *undecodable* control `g8e_missing_image` correctly draws grey `(200,200,200)×6852`. Same code path as F1, so fixing F8 would at least have made F1 audible. | Add an `else { drawPlaceholderBox(...); + LoadWarning }` to `slide_renderer.cpp:296`. |
| **9** unbounded `<a:pPr lvl>` | **CONFIRMED** | **SEV-3** | `lvl="2147483647"` and `lvl="-2147483648"` → `nonwhite=0`; `lvl="abc"` → 185 px (toInt fails → 0); `lvl="8"` → 168 px. No UB (int×double). `resolveDefaultSizePt` clamps, the renderer's `:104` does not. | `qBound(0, ..., 8)` at deck_loader.cpp:339. |
| **10** present-but-non-positive `sz` → 1 px | **CONFIRMED** | **SEV-4** | `sz="0"`, `sz="-4400"`, `sz="-inf"` → 6 non-white px vs 185 for `sz="2400"`, no warning. | Treat `sz <= 0` as absent at deck_loader.cpp:307 so master inheritance (BUG-8) applies. |

**Nothing refuted.** All ten mechanisms reproduce on my own build with my own fixtures.

**Corrections to the report**
- F1's aside *"BUG-59 is recorded as 'already fixed by BUG-41' — it is not"* is **unsupported here**: `grep -rn "BUG-5[789]" .` over the whole repo (excluding `.git`) returns nothing, and `BUGS.md` has no such row. Drop the claim or cite where it lives.
- F1's real root cause is one step deeper than stated and worth recording: the BUG-41 regression test passes because `tests/fixtures/good_pic_placeholder.pptx` declares the *layout* placeholder as `<p:pic>` — the one shape the real deck never uses. The fixture, not the code, is what let BUG-58 through. The fix must ship with a `<p:sp>`-in-layout fixture or it will regress a third time.
- F3's "52.47 s" is a floor; I measure 115.20 s render-only for the same class of deck.
- F7's "on arm64 the cast yields 0 and QFont ignores it" is right, but the report should say so in the impact line — as written it reads as picture/text loss, which I could not reproduce.

**Fix order I'd defend for a talk in 5 days:** F1 (only finding that damages the presenter's actual deck) → F8 (would have made F1 audible, ~6 lines) → F3 char cap + deadline (documented control, not implemented) → F4, F6, F9 (one-liners each) → F2, F7, F5, F10.

---

**TL;DR (plain English):** I re-ran all ten problems the first tester reported, using my own builds and my own test files rather than theirs. All ten are real — none were made up or mis-measured. The big one is genuine and serious: the photo on slide 1 of the actual presentation is silently missing, because a recent "fix" broke an earlier fix. I wrote the repair, rebuilt the app with it, and confirmed the photo comes back and all 202 existing tests still pass. Three of the other problems were rated as more serious than the evidence supports — they only happen with a deliberately sabotaged file, not with a normal PowerPoint — so I've marked those down a notch. One small side-claim in the report (about a bug number) couldn't be found anywhere in the project and should be removed.