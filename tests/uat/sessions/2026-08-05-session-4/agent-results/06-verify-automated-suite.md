## VERDICT TABLE

| # | Verdict | Agreed severity | Evidence I gathered myself | Smallest fix |
|---|---|---|---|---|
| **F1** arrow keys dropped via `mods == Qt::NoModifier` | **CONFIRMED** (and I closed the link the tester could not measure) | **SEV-1** | `CGEventCreateKeyboardEvent(NULL, kVK_RightArrow, true)` — **I set zero flags; macOS fills them in** — returns `0x20a00000`, i.e. `NSEventModifierFlagNumericPad` set on all four arrows and on keypad digits, and *not* on Space/Esc/PageDown/main-row digits. Qt 6.11.1 cocoa maps it to `Qt::KeypadModifier`. Driving the **real `PresentationWindow` + real `PresentationController`** (10-slide deck) with those OS-flagged events through `[NSApp postEvent:]`: `Right→1→1`, `Right→1→1`, `Down→1→1`, `Space→1→2`, `PageDown→2→3`, `Right→3→3`, `Left→3→3`, `Up→3→3`. Four arrow presses moved the deck zero slides. | `src/present/key_translator.cpp`, one line after `KeyAction a;` (line 34): `mods &= ~Qt::KeypadModifier;`. **A/B verified**: same probe, patched translator → `Right 1→2, Right 2→3, Down 3→4, Space 4→5, PageDown 5→6, Right 6→7, Left 7→6, Up 6→5`. All 21 key-related test cases / 161 assertions still pass. |
| **F2** blackout "any key to resume" + Esc escalation | **CONFIRMED as fact, severity OVERSTATED** | **SEV-3** (not SEV-2) | Reproduced through the real window: Esc counts 1–6 give `BLACKOUT / QUIT PROMPT / BLACKOUT / QUIT PROMPT / BLACKOUT / QUIT PROMPT`. Of 16 keys tried from the blackout, **8 resume** (4 arrows, Space, PgUp, PgDn, Backspace); Enter/P/Home/End/A/digit/Tab do nothing; Esc raises the quit prompt. `noticeForRole(HoldingHint)` returns verbatim `"Presentation paused — press Esc again to exit, any key to resume"`. Auto-dismiss is exactly 10000 ms (`onTick` at 9999 ms → prompt, at 10000 ms → blackout), matching `kConfirmQuitTimeoutMs`. Severity held at SEV-3: it is a wrong instruction line already tracked as deferred BUG-29, the presenter always has a working key, and Esc returns from the prompt instantly. **Note it is worse than reported**: 4 of the 8 resuming keys are arrows, which F1 kills on macOS — so on the real Mac only 4 of 16 resume. | Text-only, `src/present/notice.cpp:36`: name the keys ("press an arrow or Space to resume") instead of "any key". |
| **F3** failed second `openDeck` overlays start screen | **CONFIRMED as latent code defect, OVERSTATED as a finding** | **SEV-4** (unreachable in v0.1) | `src/ui/app_shell.cpp:141` does call `showStart()` unconditionally on failure — read it. But I verified unreachability independently and more tightly than the tester: `grep -rn "openDeck"` over `src/` gives exactly three call sites — `src/main.cpp:59` (launch only), and `StartView::browseRequested` / `StartView::fileDropped`. `src/ui/app_shell.cpp` calls `start_->hide()` on every successful load. `grep -rn "setAcceptDrops" src/` → **only** `start_view.cpp:32`; `grep -rn "QShortcut\|QAction\|QMenuBar" src/` → **only** `start_view.cpp:61`. No `QFileOpenEvent` handler exists. So while a deck is live there is no drop target, no Open shortcut, no menu, and no OS file-open path — the second `openDeck` cannot be invoked. | None needed for v0.1. If wired later: guard with `if (!window_) showStart();`. |
| **F4** Return/Enter does nothing on the start screen | **REFUTED** | — | Real `StartView`, real cocoa platform, OS-generated key events with full press **and release**: `Return (main) → YES`, `Enter (keypad) → YES`, `Space → YES` — all three fire `browseRequested`. Probe also prints `button hasFocus=1 autoDefault=1 isDefault=1 focusWidget=QPushButton`. The stated rationale is also wrong: `QPushButton::keyPressEvent` activates on Return/Enter whenever `autoDefault()` is true — it is not a `QDialog`-only behaviour, and `setAutoDefault(true)` at `start_view.cpp:55` makes it true. I reproduced the tester's negative result only by mis-routing the event to the `StartView` instead of the focused button, which makes **Space fail too** (0/3 fired) — a harness artifact, not app behaviour. | n/a |
| **F5** a directory reported as "That file could not be found." | **CONFIRMED** | **SEV-4** | `DeckLoader::load` on a plain folder and on a folder named `*.pptx`: both `exists=1 isDir=1 ok=0` → `describeLoadError` → `"That file could not be found."`, identical to a genuinely missing file. Reachable in v0.1: `localDeckPathFrom` (`start_view.cpp:17-27`) accepts any local URL including a directory, so dropping a folder on the start screen reaches it, as does the CLI argument. | `src/present/deck_load_worker.cpp`: add a `LoadErrorKind` for "not a file", or have the loader report `NotAZip` for a directory. |

**Two corrections to the report's framing.** The claim "no human has ever confirmed arrow navigation" understates F1's certainty — the OS half is no longer an assumption: CoreGraphics itself stamps the numeric-pad flag onto the arrow keycodes, which I measured without supplying a single flag. The residual gap is only "physical HID delivery matches CoreGraphics' own event constructor", which is also Apple's documented `NSEvent` contract. Separately, the coverage-gap section is accurate — `grep -rn "Keypad" tests/` returns nothing (rc=1), so no existing test can catch F1, and my patched relink confirms none would break.

**One caution on the harness that produced this report.** F4 was wrong in the direction of a false alarm, and its stated mechanism was wrong too. F2's "5 of 12" also does not match my 8 of 16. Both are key-routing artifacts of the harness, which is the same failure mode the report warns about elsewhere — worth weighing before trusting the unreviewed "what held" section, which I did not re-verify.

---

**TL;DR (plain English).** I checked all five complaints myself and re-ran everything.

- **The arrow-key problem is real, and I proved the last step the previous tester couldn't.** macOS itself secretly labels the arrow keys as "number pad" keys, and the app throws away any key carrying a label. I drove the actual app with actual Mac key presses: four arrow presses moved the slides zero times, while Space and Page Down worked. I then applied the one-line fix and re-ran the same test — all four arrows worked, and nothing else broke. This needs fixing before the talk.
- **The black-screen message really does overpromise**, and Escape really does put a "Quit?" box in front of the audience for ten seconds. True, but it's a wording problem with an easy workaround, so I graded it lower than the tester did.
- **The "Enter key does nothing" complaint is simply wrong** — I pressed Enter on the real start screen and it opened the file picker every time. The tester's test was aimed at the wrong part of the window.
- **The "start screen covers a live presentation" problem can't actually happen** in this version — I checked every way into that code and they're all closed off while a deck is playing. Worth a note for later, not a bug today.
- **Calling a folder "file not found" is real but trivial.**