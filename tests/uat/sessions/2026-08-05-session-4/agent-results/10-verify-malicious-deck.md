All work complete. Project tree verified untouched (`git status` clean at `89004de`; `diff -r` of `src/` and `tests/` against my pre-flight snapshot identical).

# UAT-4 skeptic pass — verdict

**Bottom line: the report's central claim survives.** I re-derived the harness from scratch (my own mutation set, my own build dir, my own probes) and reproduced **11 of 11** mutations I tried — every one behind a SEV-1/SEV-2 finding — all leaving the full 224-test suite green. My two positive controls were both killed. The flakiness rates match theirs to within 2 points.

**But the one thing the report told the team to go do is wrong.** The prescribed one-line fix for F-16 does not compile, and when made to compile it does not fix the flake.

## My setup

```
scratch = /private/tmp/claude-501/-Users-karl-.../scratchpad/uat4/sk
cmake -S sk/src-copy -B sk/b -G Ninja -DCMAKE_BUILD_TYPE=Release   # 224 tests, 100% green
sk/muts.py   — independent mutation set          sk/run.sh    — apply/rebuild/ctest/retry/restore
sk/probe.sh  — JPEG render probe                 sk/probe2.sh — srcRect render probe
sk/flaky.sh  — repeat campaigns                  sk/repro42.cpp, repro42b.cpp — instrumented race repro
```
Every mutant's build log shows the mutated TU recompiling (e.g. `out/m24.build.log`: `[41/51] Building CXX object CMakeFiles/pptv_ui.dir/src/ui/app_shell.cpp.o`), so no "survived because it wasn't rebuilt". My first harness *did* have that bug — restoring with `rsync -a` rolls mtimes backwards and ninja skips the rebuild — I caught it because a restored run still showed the previous mutant's failure. Fixed with a `touch` pass. Worth flagging in case the original harness shares it; it produces false **kills**, not false survivals, so it cannot inflate this report.

Positive controls (my own, not theirs):
```
c01 start_view.cpp localDeckPathFrom accepts remote URLs -> KILLED, 3/3 runs, only #224
c02 audio_format.hpp isPlausible channels <= 100000      -> KILLED, 3/3 runs, only #19
```

## Verdict table

| # | Verdict | Agreed sev | Evidence I gathered myself | Smallest fix |
|---|---|---|---|---|
| **F-16** root cause + rates | **CONFIRMED** | SEV-2 | Idle, 50 runs of the 19-test worker group: **8 red (16.0%)**. 10 busy loops on 12 cores, 60 runs: **17 red (28.3%)** — #42×15, #43×3, **#123×1** (so both files, as claimed). Instrumented standalone repro (`sk/repro42.cpp`, links real `pptv_core`): `iter 0 FAIL: spy.count=1 direct=1 emitAt=58ms waitEntered=58ms waitReturned=4808ms` — the spy already holds the signal, `wait()` burns the full timeout and returns false. Mechanism proven, not inferred. | see F-16-fix row |
| **F-16** prescribed fix | **REFUTED** | — | `REQUIRE(spy.count() > 0 \|\| spy.wait(5000));` **does not compile**: doctest 2.4.11 `DOCTEST_FORBIT_EXPRESSION(Result, \|\|)` → `error: static assertion failed … Expression Too Complex Please Rewrite As Binary Comparison!`. Parenthesised so it compiles, it **still fails**: 4/16 runs red under the same load, `REQUIRE( (finishedSpy.count() > 0 \|\| finishedSpy.wait(5000)) ) is NOT correct!` after 4.85 s. The window between the `count()` read and `wait()`'s internal `origCount` snapshot is still open. | Bounded poll, verified **0/60 cold starts** vs **5/60** unfixed: `QDeadlineTimer dl(5000); while (spy.count() == 0 && !dl.hasExpired()) spy.wait(20); REQUIRE(spy.count() > 0);` — best as one helper in each file, applied at `test_deck_load_worker.cpp:73,97,118,141,164` and `test_pre_render_worker.cpp:117,146,177,200,223,240,256,291` |
| **F-1** deck cannot be opened, suite green | **CONFIRMED** | SEV-1 (as risk) | m22 (delete `QShortcut` at `start_view.cpp:61-62`), m23 (`dropEvent` no longer emits, `:87`), m24 (delete both `connect(start_, …)` at `app_shell.cpp:62-63`) — each **SURVIVED 224/224 first pass**. `grep -rn AppShell tests/*.cpp tests/ui/*.cpp` returns only comments — AppShell is never constructed. `main.cpp` shows the only other way in is argv. Severity note: this is a *blind spot*, not a live break; today's binary is correct. | One widget test that constructs `AppShell`, calls `showStart()`, clicks `start_->openButton()` and asserts `browseForDeck` ran (inject the file-dialog result), plus one that emits `fileDropped` and asserts `openDeck` was reached |
| **F-2** JPEG untested, deletable while green | **CONFIRMED** | SEV-2 | m07 SURVIVED 224/224. My own render probe through the production `render_preview` on a deck I built with a real JPEG part: `pristine jpeg deck: red=9604 grey-placeholder=0` → `m07 jpeg deck: red=0 grey-placeholder=1659`; PNG deck byte-identical both ways. Reference deck structural scan: media magic = **2 jpeg-SOI, 5 png, 6 other** — the two JPEGs are genuine. All 36 fixtures: every media part is PNG or a deliberate non-PNG (EMF/GIF) negative case; largest fixture image is 60×2 px. | Add a JPEG fixture (or convert `good_image.pptx`'s part) and one renderer test asserting the picture's colour reaches the raster |
| **F-3** srcRect clamps uncovered | **CONFIRMED** | SEV-2 | m10 and m11 both SURVIVED 224/224. My probe with the reference deck's *verbatim* insets: `r="-6769" b="41679"` → pristine red=4160 green=650, m10 red=3720 green=549; `l="-6726" r="-1" b="36909"` → 4160/1104 vs 3721/915. m11 on a sub-pixel crop: pristine blue=48 yellow=49, **m11 = 0/0 — the picture vanishes entirely** (the BUG-41 class). Deck scan: **7 of 12** `<a:srcRect>` carry a negative inset (report said 6). | Two renderer tests on a fixture with a negative inset and one with a <1 px source span, asserting non-zero picture pixels in the frame |
| **F-4** audio arithmetic uncovered | **CONFIRMED**, severity **OVERSTATED** | SEV-3 | m01, m02 SURVIVED. Numbers reproduced exactly (`sk/audio_probe.cpp`): pristine `downmix 3ch: [0]=24000 [1]=200` → m01 `[0]=-29536 [1]=300`; `resample 1000 @44100 -> 363` → m02 `362`; and `44100 -> 16000` **identical** both ways, which is why the two length tests cannot see it. **But**: `grep -rn downmixToMono\|resampleMono\|toRecognizerFormat\|makeMiniaudioCapture src tools` finds **no caller anywhere outside `audio_format.cpp` and `miniaudio_capture.cpp`'s own definition**; `main.cpp` and `app_shell.cpp` contain zero audio references. The "loud click on the projector" cannot occur in the shipped binary. Real gap, zero live impact until BUG-51 wires it. | One test with `channels == 3` and one resample at a non-integer ratio (e.g. 1000 frames @44.1 kHz → 363) |
| **F-5** decode guards uncovered | **CONFIRMED**, severity **OVERSTATED** | SEV-3 | m08 (`setAllocationLimit(0)`) SURVIVED 224/224. Stronger than the mutation: the largest image in any of the 36 fixtures is **60×2 px**, so the 40 Mpx and 128 MiB caps at `slide_renderer.cpp:69-75` are unreachable by construction. Exposure is a hostile/oversized deck, not the presenter's own file. | One fixture whose PNG IHDR declares > 40 Mpx (header only, few bytes on disk) asserting the placeholder path |
| **F-6** quit hint can name a dead key | **CONFIRMED**, severity **OVERSTATED** | SEV-3 | m27 (`quitConfirmChord()` → `"Cmd+W"`) SURVIVED 224/224. Test #222 (`test_widgets.cpp:288-327`) asserts only `contains("Cmd")`, `!contains("Ctrl")`, `!contains("Shift")`, then hard-codes `QTest::keyClick(&w, Qt::Key_Q, ControlModifier)`. Confirmed by reading. Downgraded because the same prompt also offers "Esc to go back" and Cmd+Q quits from any mode (tests #219-221), so a presenter is not trapped. | Parse the key out of the hint and drive `QTest::keyClick` with **that** key instead of a literal `Key_Q` |
| **F-7** close no longer raises the prompt | **CONFIRMED**, impact claim **OVERSTATED** | SEV-3 | m26 (`presentation_window.cpp:94` `if (uiSink_)` → `if (false)`) SURVIVED 224/224; neither close-request test (`test_widgets.cpp:78`, `:265`) installs a sink. **But** the mutant suite is green *including* #219/#220/#221, which prove Cmd+Q, Dock→Quit and app-quit still terminate — the `applicationQuitInProgress()` escape at `:89-92` is untouched. m26 therefore does **not** reinstate the UAT-3 force-quit failure; it only removes the on-screen prompt on a window-close click. | Install a recording sink in the existing close test and assert `RequestQuitConfirm`/`RequestHolding` was emitted |
| **SEV-3 group** (m12-m21, m25, m28-m31) | **SPOT-CONFIRMED** | SEV-3 | Not all re-run, but the two I could falsify statically hold and are *understated*: **m21** — `deep_nest.pptx` contains **zero** `<p:grpSp>` elements, and the deepest nesting in any of the 36 fixtures is **1**, so `kMaxGroupDepth = 32` (`deck_loader.cpp:599`) is completely unexercised. **m28** — `test_audio_capture.cpp:72-95` confirmed to check only non-empty / contains "keyboard" / no `/`, `errno`, `0x` / `< 160` chars, never tying an error to its meaning. | — |

## Factual corrections to the report

- Reference deck: **7 of 12** `<a:srcRect>` carry a negative inset, not 6 (`{l:-6726,r:-1,b:36909}` ×2, `{t:-190}` ×1, `{r:-6769}` ×2, `{r:-796}` ×2).
- `tests/test_audio_format.cpp` has **11** `TEST_CASE`s, not 12. `src/ui/app_shell.cpp` is **368** lines, not ~330.
- **"Five tests in two files"** is stronger than the evidence. Across my 110 campaign runs only **#42, #43 and #123** ever failed; **#44 and #124 never did**. Their table rests on counts of 0-1 for three of the five. The *mechanism* applies to all 13 `spy.wait` sites — fix them all — but the observed flake is ~88% test #42 alone.
- Their `spyrepro` figure of 199/200 overstates the real rate by ~20×: it forces a 30 ms pre-emission. The genuine cold-start rate is 5/60 (8.3%) in my faithful repro, 8/50 (16%) for the real test.
- Verified structural claims that **do** hold exactly: 51/51 `blipFill` carry `<a:stretch>` as a direct child; `alphaModFix` amt values are `{20000, 26000, 70000}`; `miniaudio_capture.cpp` is 128 lines with no callers; max group nesting in the real deck is 1.

## The one thing to act on first

Everything in Part B is a coverage gap — nothing in the shipped binary is broken by it. **F-16 is the only finding that is costing time right now**, it has blocked three commits per `WALK-STATE.md:136`, and the fix the report handed over would have burned another cycle: it fails to compile, and the obvious repair still leaves a quarter of loaded runs red. Use the bounded poll, which I measured at 0/60.

---

**TL;DR (plain English).** I tried hard to catch the previous tester out and mostly couldn't. I re-broke eleven things in the app myself, one at a time — including making it impossible to open a presentation, and making photographs disappear — and the test suite passed all 224 tests every single time. I also broke two things it *was* supposed to catch, and it caught both, so the suite genuinely is running. The unreliability is real too: I measured one run in six failing on a quiet machine and nearly one in three on a busy one, matching their numbers almost exactly. **The important correction: the one-line repair they recommended for that unreliability doesn't work.** It won't even compile, and after fixing that it still fails a quarter of the time. I found and measured a version that does work — zero failures in sixty tries. I'd also tone down four of their severity ratings: the microphone code they're worried about isn't actually connected to the app yet, and the "you couldn't quit" scenario can't actually happen because a separate, well-tested escape route still works.