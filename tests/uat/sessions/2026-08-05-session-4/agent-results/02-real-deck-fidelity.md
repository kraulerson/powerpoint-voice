## SKEPTIC VERDICT — UAT-4 "real-deck-fidelity"

Independent harness (my own, built fresh against a clean `cmake -B .../skept/build` of the same tree): `/private/tmp/claude-501/-Users-karl-Documents-Claude-Projects-powerpoint-voice/05a4d2ab-f2bc-4839-9c01-2ee22b3c3560/scratchpad/uat4/skept/` — `skdump.cpp` (structure), `skpix.cpp` (pixels + decode-only probe), `ph.py`/`fill.py`/`count.py`/`collide.py` (XML), `mk.py`–`mk4.py` (fixtures). No real-deck text, alt-text or rendered pixel was read. Tree verified clean; `git diff cd11cf8 89004de -- src/ tests/` is empty, so code under test == cd11cf8.

| # | Finding | Verdict | Sev | My own evidence (all VERIFIED, commands run) |
|---|---|---|---|---|
| 1 | Slide 1 hero photo loads 0x0, drawn as nothing, no warning; cause = holder-namespaced `placeholderKey`; BUG-58 broke BUG-41 | **CONFIRMED** | SEV-1 | `skdump` on real deck: `S1 E01 rect=(0,0 0x0) … bytes=198219`, `warnings=0`. `ph.py`: slide1 `<pic>`/`nvPicPr` `ph{idx=11,type=pic}` vs slideLayout1 `<sp>`/`nvSpPr` same `ph`, rect `(8017727,-1 4282068x6858001)`. My own 2-file differential (identical but for layout `<p:pic>`→`<p:sp>`): control `rect=(8017727,0 4174273x6858000)` / mutant `rect=(0,0 0x0)`. `git show 571ccce^` returns `type + ':' + idx` — unqualified, so it matched pre-BUG-58 |
| 2 | Slide 5 autoshape fills discarded → 10 white runs on white; "**slide comes out blank**" | **CONFIRMED (mechanism) / OVERSTATED (impact)** | SEV-2 | `slide_model.hpp:51-54` `TextBox{rect,paragraphs}` — no fill field; loader touches `spPr` only via `parseXfrm`. My slide-5 column mirror renders `#FFFFFF 1435924` + `#373F51 1458` — the white runs and the `0098D1` arrow vanish, **but the dark body copy renders**. Contrast sweep over all 10 slides: **55 of 523 chars invisible on slide 5, 55 of 2395 deck-wide, 0 on every other slide.** Slide 5 is degraded, not blank |
| 3 | `<mc:AlternateContent>` dropped whole, silently | **CONFIRMED (defect) / OVERSTATED (severity)** | SEV-3 | My `sk_altcontent.pptx`: 2 declared, `elements=1 warnings=0`. But **0 occurrences of `AlternateContent` in the entire package**, not just the slides — zero impact on this deck today. "PowerPoint emits it for charts/ink/3D" was asserted, never run |
| 3b | `<p:sp>` with no `<p:txBody>` dropped silently | **CONFIRMED / OVERSTATED** | SEV-3 | My `sk_no_txbody.pptx`: full-width red bar declared, `elements=1 warnings=0`. `count.py` finds zero such shapes in the real deck |
| 4 | Two identical EMF pics → two stacked grey boxes at 53.6%×67.4% (BUG-39 detail) | **CONFIRMED** | SEV-3 | Both `<pic>` carry `embed=rId4`, same rect. Decode-probe: `image11.emf decodes=NO` ×2; Qt formats list has no emf/wmf. Geometry-exact mirror renders `#C8C8C8 : 496201` px (predicted box 519493 px, remainder is border/diagonal/label) |
| 5 | Count parity 51/51, 0 warnings, all 10 backgrounds correct, all srcRects round-trip, slide 9 escapes only via inline `xfrm`, fixture doesn't match the deck, 224/224 green | **ALL CONFIRMED** | — | `count.py` = 51/51 delta 0, no unknown tags. Background chain reproduced from XML for all 10 (5/6/7 → master `schemeClr=bg1`). srcRects match XML exactly incl. `l=-6726`/`t=-190`; slide 10's `<a:srcRect/>` is genuinely attribute-less. `ph.py slide9`: `<pic>` **has** inline `off/ext`, and its layout twin is `<p:sp>` — same latent break, masked. `good_pic_placeholder.pptx` layout side is `<p:pic>`. `ctest` = 224/224 |

**What I add that strengthens #1.** BUG-58's own `BUGS.md:101` row records its precondition: *"Not triggered on Karl's deck (all 45 layout placeholders sit under `nvSpPr`)"* — that is exactly the fact that makes its fix fatal to a slide-level `<p:pic>`. My `collide.py` shows the F4 collision it defends against **never occurs in any layout in this deck (0 collisions)**, while it breaks 2 real lookups (slide 1 = geometry lost, slide 9 = masked by an inline `xfrm`). And `BUGS.md:84` records BUG-41 as verified on the real deck at `(8017727,-1) 4282068x6858001` — the exact rect HEAD now fails to produce. So this is a proven regression, not a mis-diagnosis. BUG-59's "already fixed by BUG-41" is untested, as claimed.

**Smallest fixes (CONFIRMED items).**
- **#1** — `deck_loader.cpp`: make the lookup fall back. In `parseLayoutPlaceholders` (~586) also `map.insert(type + ':' + idx, rect)` when that key is absent; at the two consumer sites (631-633, 647-649) try the holder-qualified key first, then the unqualified one. Keeps BUG-58/F4 disambiguation (qualified still wins), restores BUG-41. ~6 lines. Then re-point the fixture's **layout** side to `<p:sp>` so `test_deck_loader.cpp:555` actually pins the real deck's shape (closes BUG-55).
- **#2** — add `std::optional<Color> fillColor` to `TextBox` (`slide_model.hpp:51`), parse `<a:solidFill>` from `<p:spPr>` in `parseTextBox` (already reads `spPr` at :324), and `p.fillRect(rect, fill)` before layout in `drawTextBox`. Restores contrast and the coloured chrome; geometry stays rectangular. ~10 lines.
- **#3/#3b** — add `else if (name == "AlternateContent")` recursing into `<mc:Fallback>` (else `<mc:Choice>`), and in the `sp` branch emit a `LoadWarning` instead of silently `continue`-ing when `txBody` is absent.

**Severity corrections.** #2 down to SEV-2: the mechanism is real and contract-violating (F1's "never a silent wrong render"), but the slide keeps its title, its five body-copy blocks and its step numbers — 89.5% of its characters render. #3/#3b down to SEV-3: genuine silent-drop defects, but zero occurrences anywhere in the package, so on the *real-deck-fidelity* surface they cost nothing today.

**One gap the report understates.** An *undecodable* image gets a placeholder box but **still no `LoadWarning`** — the loader never decodes, so no load-time warning is possible. My `sk_slide1_mirror` renders two grey boxes at `warnings=0`. F1's contract is "visible placeholder **+** triage-shaped warning list"; only half of it is met for BUG-39's EMFs.

**Bottom line.** The report's headline is right and its central SEV-1 is fully reproduced, root-caused and now shown to be a regression documented against itself. Its second SEV-1 is a real defect wrapped in an inaccurate impact claim ("blank slide") that my pixel and contrast evidence refutes, and its SEV-2 is a zero-occurrence latent bug rated one level too high.

---

**TL;DR** — I re-ran the other tester's work from scratch with my own tools instead of trusting theirs. The big one is real: on the opening slide, the main photograph loads with a size of zero and is simply never drawn, and nothing warns you. I traced why — a change made last round to fix a rare problem accidentally broke this common one, and the project's own bug notes even record the detail that made it break, plus the correct size the picture used to get. I also checked whether that rare problem ever actually happens in this deck: it never does. The second claim is half right — the app does throw away the coloured boxes behind shapes, and five white headings on slide 5 do vanish into the white background, but the slide does *not* come out blank; the title, the body text and the step numbers all still show. So I downgraded that one. The warning about a future PowerPoint feature is a genuine bug but it appears nowhere in the deck, so I downgraded that too. I also spotted something the tester missed: the two broken graphics on slide 1 draw grey error boxes but generate no warning either, so the app's promise of "always tell the user what it couldn't handle" isn't fully kept. I changed nothing in the project.