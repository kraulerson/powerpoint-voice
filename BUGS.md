# Bug Tracker

<!--
  This file tracks bugs found during UAT sessions and ad hoc testing.
  Status and severity patterns are read by scripts/test-gate.sh for phase gate checks.
  Do NOT change the table format — the column order and status values are parsed by scripts.
-->

| # | Severity | Status | Feature | Description | Session | Disposition | Fix Reference | Verified In |
|---|---|---|---|---|---|---|---|---|
| 1 | SEV-1 | Fixed | F1a/F1b | Text color read only from `<a:srgbClr>`; real decks use theme/scheme colors (`<a:schemeClr>`) or inherit from the master → no color → black default → INVISIBLE text on dark exec slides | Session 1 | Fix Now | theme parse + schemeClr resolve + luminance default | Session 1 |
| 2 | SEV-1 | Fixed | F1a | Placeholder text with no inline `<a:xfrm>` (position inherited from slideLayout/master) collapses to 0,0,0,0 → boxes overlap top-left and cx=0 clips text away | Session 1 | Fix Now | slideLayout placeholder geometry + default content-rect fallback | Session 1 |
| 3 | SEV-2 | Fixed | F1a/F1b | Grouped shapes `<p:grpSp>` treated as Unsupported → grey placeholder box hides all grouped text/content | Session 1 | Fix Now | recurse into grpSp children | Session 1 |
| 4 | SEV-2 | Fixed | F1b | Long text does not wrap (`Qt::TextSingleLine`) → bullets silently truncated at box edge, no ellipsis | Session 1 | Fix Now | QTextLayout WordWrap | Session 1 |
| 5 | SEV-2 | Fixed | F1b | Multi-run paragraph renders in the first run's color/font only; per-run color/bold/italic/size of later runs lost | Session 1 | Fix Now | QTextLayout per-run FormatRange | Session 1 |
| 6 | SEV-2 | Fixed | F1a | Line breaks `<a:br>` dropped; runs concatenate ("Q3⏎FY26" → "Q3FY26") | Session 1 | Fix Now | `<a:br>` → U+2028 separator | Session 1 |
| 7 | SEV-3 | Fixed | F1a/F1b | Bullet markers (`buChar`/`buAutoNum`) and list-level indentation ignored | Session 1 | Fix Now | parse buChar/lvl + render bullet prefix/indent | Session 1 |

| 8 | SEV-1 | Fixed | F1a/F1b | Runs with no inline `sz` rendered at 0→1px (blank/tiny text) — real decks inherit font size from the master `<p:txStyles>` (title/body/other per level) | Session 1 (real deck) | Fix Now | parse master txStyles + resolve default size by placeholder type/level + fallback | Session 1 |
| 9 | SEV-2 | Won't Fix (MVP) | F1b | EMF and WDP (JPEG-XR) images render a "missing image" placeholder — Qt has no decoder for these Windows vector/HD-Photo formats | Session 1 (real deck) | Post-MVP | none feasible without a metafile engine; **workaround: re-export EMF/WDP images as PNG in PowerPoint** | — |
| 10 | SEV-3 | Fixed | F1b | Image drawn stretched-to-fill its frame; a frame whose aspect differs from the image looks squished (matches PowerPoint's fill default in most cases; a source-crop/`srcRect` is not applied) | Session 1 (real deck) | Defer | investigate `srcRect` / aspect after the font-size fix re-check | Superseded by BUG-53 and BUG-37: `<a:stretch>` is honoured (fill when present, aspect-preserved when absent) and `<a:srcRect>` is applied. This row's own note — that stretching "matches PowerPoint's fill default" — turned out to be right and sat deferred for four sessions while the wrong behaviour shipped |

| 11 | SEV-1 | Fixed | F2/F3 | Stuck-in-Paused: once paused for Q&A, ONLY the literal "continue presentation" resumes; "resume", "continue", "continue the presentation", "okay let's continue" are all dropped while nav stays gated → deck frozen mid-talk with no voice recovery | Session 2 | Fix Now | resume synonyms ("resume"/"continue"/±"the presentation") in matchCommand, test-first | Session 2 |
| 12 | SEV-2 | Fixed | F2/F3 | Strict equality on the 4 fixed commands drops common natural phrasings — "okay next slide", "next slide please", "so previous slide", "pause the presentation", "lets pause" → (no command); plus a "please" asymmetry (works for "go to slide N please" but not the fixed commands) | Session 2 | Fix Now | small SAFE leading/trailing filler strip + optional "the" on control phrases; re-audited (audience sentences still no-command), test-first | Session 2 |
| 13 | SEV-3 | Deferred | F2/F3 | Directional aliases miss: "go to the next slide", "move to the next slide", "can we go to the next slide" → (no command). Deferred to the voice-engine feature: collision-prone with audience speech, best designed with the grammar-constrained recognizer | Session 2 | Defer | voice-engine feature (design with Vosk) | — |
| 14 | SEV-3 | Deferred | F4 | Natural spoken numbers miss/surprise: ordinals ("twenty-fifth", "the fifth slide"), "one oh five" (oh=zero), "last" → (no command); and digit-by-digit "go to slide five and six" → GoToSlide(56). Fails safe (needs the literal "go to slide" prefix) | Session 2 | Defer | voice-engine feature | — |
| 15 | SEV-3 | Deferred | F2/F3 | The M-MED-1 edge-punctuation strip covers only ASCII; typographic "…" (U+2026) / curly quotes (macOS/iOS dictation auto-substitutes) are not stripped → "next slide…" → (no command). Fails safe; identical on macOS/Ubuntu | Session 2 | Defer | voice-engine feature (broaden to Unicode punctuation) | — |
| 16 | SEV-3 | Deferred | F2/F3, F4 | Matcher emits out-of-range/degenerate slide numbers ("go to slide zero"→0, "-5"→5, digit path bypasses the word path's kMax=100000) — leans entirely on the F7 caller to clamp. F7 MUST range-check 0/negative/over-length before ship (per the F4 caller-range-checks contract) | Session 2 | Defer | F7 clamp requirement + F4 digit-path cap consistency | — |
| 17 | SEV-2 | Fixed | F2/F3 | Bare single-word commands ("resume"/"continue"/"pause") were accepted, so ONE conversational word re-armed navigation during Q&A — the exact window the Paused state protects (TM-002/019). The BUG-11 filler strip made it worse: "okay lets continue", "and now continue", "lets pause" all reduced to a lone command word and fired. Regression introduced by the BUG-11/12 UAT-2 remediation | Design review (voice-engine) | Fix Now | require the object: "pause/continue/resume (the) presentation"; bare words rejected. 7 regression assertions. Residual stuck-in-Paused risk covered by F6 keyboard parity, resequenced BEFORE the voice engine | Session 3 (pending) |
| 18 | **SEV-1** | Fixed | F7 | **The application could not load a deck at all through its own UI.** `StartView` shipped as two labels and nothing else — no button, no shortcut, no drag-and-drop — so a deck could only be supplied as a **command-line argument**. Anyone who launched the app normally got a dark window and no way forward. `QFileDialog` was `#include`d in `app_shell.cpp` and never called. **Karl hit this on the first build he was given for his MacBook Pro**, which is the only way it could have been found: nothing in 211 tests asserted the app was USABLE, and every agent test drove it from the command line | Session 1 (logged), **hit by Karl 2026-08-05** | Fix Now | a deck-loading button + the platform `QKeySequence` load shortcut (Cmd+O on macOS, Ctrl+O elsewhere — resolved per platform rather than hard-coded, which is the mistake that made the quit chord untypeable in BUG-35/43) + drag-and-drop. The drop decision is a PURE FUNCTION so the local-vs-remote rule is testable without Qt's DnD machinery | 216 tests green; GROUP S pins the button, the shortcut and the rule that a REMOTE url is refused — this app has no network path and a drop handler must not create one |
| 19 | SEV-3 | Deferred | F7 | Audit F7a deferred items: `noticeForRole()` audience/operator split implemented for only 2 of 11 notice ids (TM-012's mitigation is "split overlay by display role"), and it has no caller or tests yet; `setDeck()` resets to slide 1, forces Presenting and never clears `quitConfirmed_`; cosmetic notice defects ("Deck has 1 slides", unused args, gate ordering) | Design review (F7a audit) | Defer | F7b — the sub-feature that introduces the display surfaces and the first caller | — |
| 20 | SEV-3 | Deferred | F7 | TM-002's specified command rate limiting (3 per 5 s, >=700 ms apart) and reverse-direction confirmation are absent: `dispatch()` takes no timestamp so it structurally cannot throttle. The recorded-playback replay path is unthrottled. Not a defect in F7a — it is assertion group F, scheduled for a later F7 stage — but logged so the audit trail does not imply TM-002 is fully mitigated | Design review (F7a audit) | Defer | later F7 stage (rate-limiting group) | — |
| 21 | SEV-2 | Fixed | F7 | TM-018 PREVENT measured the wrong quantity: the caps count shapes and text runs only, so under-cap slides still take minutes (measured: 2000 pictures of one 31 Mpx image ~309 s; 5000 runs x 300k chars ~657 s). ISOLATE holds so the UI never blocks, but the slide never appears | F7b audit | Defer | F7c — add total characters and total DECLARED image pixels to SlideComplexity, completing the ratified four-cap set (TM-018.3-A) | — |
| 22 | SEV-2 | Fixed | F7 | The raster cache was unbounded: 3840x2160 RGB32 ~31.6 MB/slide x 300 slides = ~9.27 GB, which would swap then OOM-kill the showtime machine. Auditor judgement: survivable under ~120 slides at 4K. **Karl's deck is 10 slides (~316 MB) — NOT a risk for the 2026-08-10 talk**; this is hardening for arbitrary decks | F7b audit | Defer | F7c — the ratified always-on 2 GB window (Bible section 3 A3-1(3) / B1-A). STOPGAP if needed: clamp renderTargetPolicy to 1080p (~7.9 MB/slide, ~2.3 GB at 300 slides) | — |
| 23 | SEV-3 | Deferred | F7 | F7b audit polish set: isPlaceholder is discarded so a last-resort 1x1 raster renders as a black square with no notice (M3); SurfaceState::HoldLastGood is never used so jumping to an un-rendered slide flashes the projector black (M4); std::move on a const LoadResult selects the copy ctor (L1); window_/start_ are parentless and never deleted (L2) | F7b audit | Defer | F7c | — |
| 24 | SEV-1 | Fixed | F7 | Esc privacy blackout un-blanked ITSELF: AppShell::onSlideReady called showSlide() with no mode check, so the pre-render worker painted the Confidential deck back onto the blanked projector 1-3 s after Esc (measured: 56.2% of projector = deck pixels while mode==Holding) and also wiped the quit prompt. The earlier H3 fix gated refresh() but left the raster path ungated — an INCOMPLETE fix | Session 3 | Fix Now | onSlideReady now calls the mode-aware refresh(); showSlide() early-returns unless Presenting; 2 regression tests incl. the first AppShell-level coverage | Session 3 |
| 25 | SEV-2 | Fixed | F7 | Double letterboxing on MacBook+projector: renderTargetPolicy picks the largest screen by DEVICE pixels (Retina laptop 3024x1964, aspect 1.54) over the projector, SlideRenderer bakes bars into the raster at that aspect, and the surface boxes it AGAIN against the 16:9 window -> the deck covered 75% of the projector with 13% smaller text for the whole talk. Invisible in rehearsal (needs a second screen of a different aspect) | Session 3 | Fix Now | new renderTargetForDeck(): picks the presentation (non-primary) screen and fits the DECK's aspect, so the first letterbox is a no-op; 3 regression tests | **VERIFIED ON HARDWARE by Karl, 2026-08-05**: projector tested, reported good. Fixed by inspection in session 3; this is the first confirmation on a real second display |
| 26 | SEV-2 | Fixed | F7 | teardownWorkers() could SIGABRT or deadlock when a slide render outlives the 5 s wait — destroying a running QThread is a qFatal abort, and terminate() can strand allocator locks. The earlier H2 fix still risked it | Session 3 | Fix Now | if the wait expires, DETACH and deliberately leak the thread (parent cleared) — a leaked thread at shutdown is strictly better than aborting in front of a room | Session 3 |
| 27 | SEV-2 | Fixed | F1a | Unbounded <p:grpSp> recursion stack-overflowed the load worker: a 5.7 KB hostile deck killed the app instantly with no dialog and no stderr | Session 3 | Fix Now | kMaxGroupDepth=32; content below the cap is dropped with a LoadWarning rather than followed | Session 3 |
| 28 | SEV-2 | Fixed | F7 | A failed open on the CLI launch path left NO window and the app exited silently — the audit-C3 'never leave the presenter with no window' guard was dead code there (start_ is null on that path) | Session 3 | Fix Now | call showStart(), which CREATES the view, instead of the dead `if (start_)` guard | Session 3 |
| 29 | SEV-3 | Deferred | F7 | UAT-3 SEV-3/SEV-4 set (19 items), full detail in tests/uat/sessions/2026-08-05-session-3/submissions/uat-3-triage.md: Notices never expire: NoticeClass::Transient is set on every notice and read by nothing, so a message band sits on the projector for the rest of the talk; Ctrl+Shift+F (ToggleFullScreen) and Ctrl+Shift+R (ReRenderDeck) are consumed by the key translator and then silently dropped by AppShell; Typed slide numbers give the presenter zero feedback, and the translator's 'slide number too long' notice has no route to any surface; The privacy-blackout screen shows the audience an operator instruction, and the instruction is factually wrong ('any key to resume'); Keyboard focus is not restored to the presentation window after the modal error dialog closes, and the dialog is unparented; 8 of the 178 ctest entries execute ZERO test cases — and the 4 real cases they should run cover the blackout, the two-step quit, BUG-16 and BUG-11/17; The 1 GB decompression cap is charged per zip entry but slide/... | Session 3 | Defer | F7c and later sub-features; none blocks the 2026-08-10 talk on a 10-slide deck | — |
| 30 | SEV-1 | Fixed | F1b/F7 | **App SEGFAULTS on Karl's real deck.** *(Root cause CORRECTED 2026-08-05 — my first diagnosis, an EMF image-plugin probe, was a hypothesis I never reproduced and it was WRONG.)* The macOS crash report `powerpoint_voice-2026-08-04-190008.ips` names it exactly: faulting thread 5 is our pre-render `QThread`, inside `QFontDatabasePrivate::findFont` -> `initFontDef` -> `QString::operator=`, while the MAIN thread is inside `QApplicationPrivate::handleThemeChanged()`. It is a data race on Qt's global font/theme state: `handleThemeChanged()` clears and re-initialises the application font, and `app_shell.cpp` started the render worker at line 206 and then created and showed the fullscreen window at 208-224 — showing a widget window is what fires that handler. Aggravator: the deck names ~50 font families (Mulish, Century Gothic, Segoe UI, Estrangelo Edessa, ...) and almost none exist on macOS, so nearly every text run takes the slow full-fallback path through the font database, widening the window enormously. Explains why the LibreOffice-converted PNG deck survived (fonts rewritten to installed ones), why `render_preview` never hit it (no widgets, no theme change) and why 25 headless repro runs here did not | Session 3 (Karl, human) | Fix Now | 3 GUI-thread mitigations before any worker exists: set the application font EXPLICITLY (Qt skips the theme-change font reset when the app claims it); populate `QFontDatabase::families()` up front; and START THE RENDER WORKER ONLY AFTER the window is shown, via a queued call, so it never overlaps the trigger. Residual risk -> BUG-34 | **Verified by Karl on the MacBook Pro 2026-08-06**: both the original and the -png deck loaded and ran, no crash. Root cause was the Qt font/theme data race, not the EMF image path first recorded |
| 31 | SEV-1 | Fixed | F7 | **The app could not be quit by any graceful means.** *(First fix was INSUFFICIENT — it made a close request raise the quit prompt, but the request was still `ignore()`d, so the application still refused to terminate.)* Root cause, now VERIFIED and REPRODUCED: Qt documents that `QCoreApplication::quit()` "may be ignored if the application prevents the quit, for example if one of its windows can't be closed" — an application quit (Dock->Quit, Activity Monitor->Quit, Cmd+Q) is delivered by asking every top-level window to close, and Qt CANCELS the shutdown if any window refuses. `closeEvent` refused every close request, from every source, so only SIGKILL (Force Quit) worked. Reproduced outside the suite with `NSRunningApplication::terminate()` — literally what Activity Monitor's Quit button calls — against a real PresentationWindow: request delivered, process survived | Session 3 (Karl, human) | Fix Now | tell the two requests apart. An application-level `QEvent::Quit` (the point Qt documents for this) now closes the windows with an 'application is quitting' flag raised, so the window accepts from ANY mode with no prompt; an ordinary window close is still refused and still raises the two-step prompt. Flag is scoped to that one call, so it never leaks | **Verified by Karl 2026-08-06**: "cmd+Q working properly". Application quit is obeyed from any mode; an ordinary window close is still refused |
| 32 | SEV-2 | Fixed | F1a/F1b | **All slide backgrounds render white.** NO slide carries its own `<p:bg>` — the background lives on the master (1/1) and 12 of 17 layouts. The loader read only slide level, so nothing was inherited. Same bug class as BUG-1 (theme colours) and BUG-2 (placeholder geometry) | Session 3 (Karl, human) | Fix Now | resolve `<p:bg>` up the slide -> layout -> master chain (layouts and masters parsed once each and cached), including `<p:bgRef>` into the theme's `bgFillStyleLst`; a declared-but-unpaintable fill now WARNS instead of silently rendering white | Verified on the real deck: all 10 slides resolve, 0 warnings — `373F51/0076A3/EAF0F6/FFFFFF x4/0076A3/EAF0F6/0098D1`, matching its layout+master structure exactly. 4 of them are DARK, so this likely also fixes white-on-white text |
| 33 | SEV-3 | Open | F7 | Slides render "a bit slowly" on the real deck (Karl, over RustDesk). Now partly quantified: the pre-render itself is NOT slow — all 10 slides rasterise off-thread in ~200 ms at 1920x1080 on this machine. Remaining suspects: the ~50 missing font families each taking a full fallback search on first use, the 4K render target on the real projector, and RustDesk's own frame transport | Session 3 (Karl, human) | Defer | F7c — resolve deck font families to installed families ONCE on the GUI thread (also shrinks BUG-30's residual race), then re-measure at the real projector resolution | — |

**Known remaining limitations (documented, not blocking, tracked for post-MVP):** theme resolution uses `theme1.xml` + default color map; placeholder geometry inherits one level (slideLayout); group child transforms not applied; inline bullets only; **EMF/WDP images unsupported (BUG-9 — re-export as PNG)**; font-size inheritance resolves via the master txStyles (not per-layout overrides). Text is now **readable and correctly sized/colored** on real decks.
<!--
  Severity: SEV-1, SEV-2, SEV-3, SEV-4 (see PROJECT_BIBLE.md Bug Severity Classification)
  Cite bugs elsewhere as BUG-<#> (the # column, bare integer, no zero-padding — see docs/IDENTIFIERS.md)
  Status: Open, Fixed, Deferred, Won't Fix, Post-MVP, Removed
  Disposition: Fix Now, Defer, Won't Fix, Post-MVP (assigned during triage, Step 2.8)
  Session: UAT session number where the bug was found (e.g., "Session 4")
  Fix Reference: PR number or commit hash of the fix (e.g., "PR #12" or "abc1234")
  Verified In: UAT session number where the fix was verified (e.g., "Session 5")
-->

## Status Guide

| Status | Meaning |
|---|---|
| **Open** | Bug confirmed, not yet fixed |
| **Fixed** | Fix implemented and verified |
| **Deferred** | Tracked with justification — must be resolved or feature removed at Phase 2→3 gate |
| **Won't Fix** | Accepted as-is with documented rationale (SEV-3/4 only) |
| **Post-MVP** | Moved to post-MVP backlog (SEV-4 enhancements only) |
| **Removed** | Feature containing the bug was removed |

## Severity Guide

| Severity | Definition | Examples | Can Defer? |
|---|---|---|---|
| **SEV-1** | Data loss, security breach, app crash on core flow | Auth bypass, database corruption, crash on login | No — must fix immediately |
| **SEV-2** | Feature broken but workaround exists, significant UX failure | Form submits wrong data, layout broken on one platform | Yes — but must resolve or remove feature at Phase 2→3 gate |
| **SEV-3** | Minor UX issue, cosmetic, non-core edge case | Alignment off, tooltip truncated, rare edge case | Yes |
| **SEV-4** | Enhancement, suggestion, polish | "Would be nice if...", performance optimization | Automatic Post-MVP |
| 34 | SEV-2 | Fixed | F7 | **Residual of BUG-30:** the pre-render worker still uses Qt's font database off the GUI thread, so a theme change that lands WHILE the deck is pre-rendering can still race it — macOS switching to dark mode at sunset, a display being attached, a RustDesk session reconnecting. The BUG-30 fix removes the one trigger we control (showing our own window) but not spontaneous ones. Exposure is the pre-render duration: ~0.2 s for Karl's 10 slides, proportionally longer for a big deck | BUG-30 analysis | Defer | F7c — resolve every family the deck names to an installed family ONCE on the GUI thread and hand the renderer concrete families, so the worker never enters font fallback; consider rendering text with pre-resolved QRawFont | `QApplication::setDesktopSettingsAware(false)` before the application is constructed removes the TRIGGER CLASS rather than narrowing the window: Qt no longer reacts to dark mode at sunset, a display attaching, or a remote-desktop reconnect — the three spontaneous triggers that could not be preempted, because the invalidation happens inside Qt before any handler of ours runs. Safe because the app has no desktop-theme integration to lose: a fixed minimal-dark shell (Bible section 9) and slide colours that come from the DECK |
| 35 | SEV-3 | Fixed | F7 | **The quit prompt named a chord that does not exist on macOS.** It said "Ctrl+Shift+Q", but Qt maps the COMMAND key to `Qt::ControlModifier` on macOS and the physical Control key to `Qt::MetaModifier`, so a Mac user following the hint literally presses Control+Shift+Q, which arrives as Meta|Shift and matches nothing. Karl reported the chord "didn't work" and it could not have | Session 3 (Karl, human) | Fix Now | `quitConfirmChord()` returns "Cmd+Shift+Q" on macOS and "Ctrl+Shift+Q" elsewhere; a test asserts the hint names the chord the translator actually accepts | Superseded by BUG-43: the macOS hint is now `Cmd+Q`, deliberately not `Cmd+Shift+Q` (the system Log Out shortcut). A test asserts the hint names the chord the translator accepts |
| 36 | SEV-3 | Won't Fix — By Design | F7 | **Design question for Karl, not a defect:** now that an application quit is obeyed, **Cmd+Q terminates immediately mid-talk with no confirmation** (macOS routes Cmd+Q through the menu to the same application-quit path). That is the standard platform contract and it is deliberate — two keys, one of them Command — but it is a change from "Cmd+Q did nothing". The alternative is to gate Cmd+Q behind the two-step prompt while still obeying Dock/Activity Monitor quits | BUG-31 fix | **Karl's call** | ask before the live talk | Karl's ruling 2026-08-05: "Cmd+q is a deliberate key press. Leave it to quit immediately." Matches the macOS platform contract |
| 37 | SEV-2 | Fixed | F1a/F1b | **`<a:srcRect>` source cropping was ignored — pictures were drawn whole.** Karl: "On Slide 1, there is a graphic that should have a transparent background to only show the geometric shape... currently has a white background with the geometric shape in it." His slide-1 JPEG is cropped **29.178% off EACH side** in the deck; we drew all of it, so the margins PowerPoint discards came along. Slide 9's picture is cropped 31.6%/40.9% and was equally wrong. A JPEG cannot carry transparency, so the white is IN the pixels — the deck's answer is to crop it away, and we were not doing that | Session 4 (Karl, human) | Fix Now | parse `<a:srcRect>` from the `<p:blipFill>` (direct child, not a descendant search) and pass it as the source rect to `drawImage`; negative insets clamp to the image bounds | Verified on the real deck: slide 1 img 1 l=29178 r=29178, slide 9 img 1 l=31593 r=40879, both now parsed |
| 38 | SEV-3 | Fixed | F1a/F1b | `<a:alphaModFix>` picture opacity was ignored, so a picture the deck wants washed out rendered fully opaque. Slide 1's two EMF graphics are 70% | Session 4 | Fix Now | read `amt` and set `QPainter::setOpacity` for that draw only, restored immediately so one translucent picture cannot wash out everything after it | 196 tests green |
| 39 | SEV-3 | Open — **needs Karl's eyes again** | F1b | Slide 1 of the ORIGINAL deck contains two EMF graphics we cannot decode, drawn as the grey diagonal-cross "missing image" placeholder at 53% x 67% of the slide. Karl reports he does NOT see them, which I still cannot explain — the loader confirms both elements exist with real geometry and 2188 bytes of EMF each | Session 4 | Ask Karl | after BUG-41 lands, ask him to look at slide 1 again and say exactly what is in the middle of it | — |
| 40 | SEV-2 | **Partly fixed — residual NOT diagnosed** | F7 | **Flaky worker-thread tests.** A UAT-4 tester measured the real scope I had never established: **5 tests across TWO files** (`test_deck_load_worker.cpp` AND `test_pre_render_worker.cpp` — nobody had looked at the second), failing **19% of clean full-suite runs and 30% under load**. Root cause VERIFIED by isolated reproduction: `QSignalSpy::wait()` counts only emissions arriving DURING its event loop, so a worker that emits BEFORE the main thread reaches `wait()` is invisible and the wait burns its full timeout. The tester measured 199 of 200 iterations hitting exactly that | Sessions 3-4 | Fix Now (partial) | `REQUIRE((spy.count() > 0 \|\| spy.wait(N)))` at all 13 sites — check the count FIRST. **Measured 15-30% -> 2.5%.** | **NOT closed.** "P: every failure kind survives the thread boundary" still fails ~2.5% of 80 runs; polling to a 15 s deadline did NOT change that, so the residual is neither the spy race nor scheduling delay and I have not diagnosed it. I have re-run past this group three times; stating the residual rather than claiming a fix |
| 41 | SEV-2 | Fixed | F1a | **A picture placeholder was silently dropped — slide 1's main photograph never rendered at all.** PowerPoint writes a picture dropped into a layout's picture placeholder as a `<p:pic>` with `<p:ph type="pic" idx="11"/>` and **no `<p:spPr>`**; the layout positions it. `placeholderKey()` looked for `<p:ph>` only under `<p:nvSpPr>`, which a `<p:pic>` does not have, so it matched no layout entry, stayed 0x0, and the renderer's `r.width() >= 1` guard skipped it. No warning — the picture was simply absent. This is why my reading of slide 1 was wrong twice | Session 4 (found while investigating BUG-39) | Fix Now | look for `<p:ph>` under nvSpPr/nvPicPr/nvGraphicFramePr/nvCxnSpPr; index `<p:pic>` placeholders in the layout too; fall back to the layout rect when a picture has no geometry | Verified on the real deck: slide 1's picture went from `(0,0) 0x0` to `(8017727,-1) 4282068x6858001` — the right-hand third, full height |
| 42 | SEV-2 | Fixed | F7 | **Quitting can take up to 5 s, and further quit requests in that window are silently discarded** — from Activity Monitor that is indistinguishable from a refusal. Measured by an adversarial reviewer with `/usr/bin/sample`: after `exec()` returns, `~AppShell` -> `teardownWorkers()` -> `t->wait(5000)` parks the GUI thread with NO runloop, so `applicationShouldTerminate:` is never invoked again (`qcocoaapplicationdelegate.mm` states it only runs while NSApp is running). They built a 1-slide deck with 1990 image shapes — **under both TM-018 caps** — that takes 13.6 s to rasterise, so the wait genuinely expires | Workflow adversarial review, Session 4 | Defer | F7c — give `PreRenderWorker::renderOne` a cancellation point, or on the quit path skip straight to `_exit(0)` once the worker is confirmed detached. NOTE: a reviewer PROVED the tempting fix (don't wait at all, just detach) causes SIGSEGV in 8/14 runs — `~QGuiApplication` tears down the font database while the render thread is still inside `QFont`/`QPainter`. The current 5 s wait is the safe behaviour; do not remove it without replacing it | **Measured 5000 ms -> ~303 ms**, clean exit 6/6 with a slide genuinely mid-render. On the quit path the wait drops to 300 ms and a still-running worker exits via `_exit(0)`, which skips the static destructors that would otherwise race the render thread. Safe here precisely because TM-011 forbids a disk cache, so there is nothing to flush |
| 43 | SEV-3 | Fixed | F7 | **The quit prompt advertised Shift+Cmd+Q — the macOS system "Log Out" shortcut.** Introduced by my own BUG-35 fix. Printing it on a projector invites the presenter to log the machine out mid-talk. It is also unnecessary: per Karl's BUG-36 ruling Cmd+Q now quits from any mode | Session 4 (self, after the workflow review) | Fix Now | macOS hint is now "Cmd+Q", which the translator already accepts (it matches ControlModifier+Key_Q and ignores Shift) and which AppKit routes to the application-quit path the filter obeys. A test asserts the macOS hint contains no "Shift" | 197 tests green |
| 44 | SEV-3 | Open | F7 | A logout/restart/shutdown Apple Event terminates the presentation with no confirmation, exactly like an explicit quit — the `QEvent::Quit` filter cannot see who asked. Correct for a real shutdown; a hazard if a system update prompt fires mid-talk | Workflow adversarial review | Defer | F7c — consider `QGuiApplication::commitDataRequest`/session-management to distinguish a logout from a user quit, if Qt exposes it on macOS | — |
| 45 | SEV-1 | Fixed | F-voice | **The app would have been KILLED BY macOS the first time voice was armed.** `MACOSX_BUNDLE ON` makes CMake generate a default Info.plist with no `NSMicrophoneUsageDescription`; macOS terminates a process outright — not an error return, not a catchable dialog — when it opens an audio input device without one. It would have failed ONLY on the presenter's MacBook Pro: the dev Mac mini has no microphone to trigger it, so no amount of testing here would have found it | Karl's deployment constraint, Session 4 | Fix Now | `cmake/MacOSXBundleInfo.plist.in` supplies the key; verified present in the generated bundle with `plutil` | Verified in `build/powerpoint_voice.app/Contents/Info.plist` |
| 46 | SEV-2 | Fixed | F-voice | **Assuming the capture format would have produced confident nonsense on the showtime machine.** Vosk needs 16 kHz mono; a MacBook Pro's built-in mic is a 3-element array CoreAudio typically presents at 48 kHz, and a headset can be 44.1 kHz stereo. Feeding 48 kHz to a 16 kHz model does not error — it decodes 3x-speed audio as plausible words, which is the worst failure mode for a command recogniser | Karl's deployment constraint | Fix Now | `src/audio/audio_format.*`: the device reports its format and we downmix + resample. Multi-channel is AVERAGED, not channel-0-sampled (the array elements are not equivalent). 9 GROUP AF tests including a sine-shape assertion, because a length-only check passes for silence | 206 tests green |
| 47 | SEV-1 | Fixed | F1a | **A picture saved in ISO 29500 STRICT format became completely INVISIBLE.** `<a:alphaModFix amt="50%"/>` and `<a:srcRect l="25%"/>` are the Strict spellings, written by PowerPoint's own "Strict OOXML Presentation" save option. `QString::toInt()` returns **0** for them without reporting failure — and 0 means fully transparent for alpha and no-crop for srcRect. Introduced by MY OWN BUG-37/38 fix: a brand-new way to silently lose a picture, in the same change set whose sibling commit (BUG-41) exists because a picture was silently lost | **Adversarial PR review** (the one Karl asked for), Session 4 | Fix Now | `parsePercentAttr()` accepts both spellings and REPORTS failure; an unreadable opacity draws OPAQUE (never transparent) and an unreadable crop shows the whole picture, both with a LoadWarning | Reproduced by the reviewer on a 6-picture fixture; now pinned by mutation-tested assertions |
| 48 | SEV-2 | Fixed | F1b | **An over-crop drew a MIRRORED region the deck explicitly excluded.** When `l+r > 100000` the computed width is negative, and `QRectF::intersected` **normalises** it — swapping the edges and returning a valid rectangle covering exactly the excluded region. The downstream `src.width() < 1` guard never fired because the normalised width is positive | Adversarial PR review | Fix Now | reject insets that meet or cross, in `sourceRect()`, before any rectangle is built | Verified: removing the guard puts 3915 red pixels where none belong |
| 49 | SEV-3 | Fixed | F-voice | **`libvosk` could not be dynamically loaded at all.** Its `LC_ID_DYLIB` is the bare leaf `libvosk.dylib`, and dyld expands `LC_RPATH` only for install names beginning with `@rpath/` — so no rpath is ever consulted and the process dies at start, bundle or not. Upstream gets away with it because Python's ctypes loads by absolute path, bypassing install-name resolution; C++ linking does not | Adversarial PR review | Fix Now | `cmake/VendorVosk.cmake` prepares a COPY at build time: `install_name_tool -id @rpath/...` then `codesign -f -s -` (mandatory on arm64 — install_name_tool invalidates the signature and an invalid one fails with a DIFFERENT error). The vendored artifact and its SHA-256 provenance are untouched | **Verified side by side**: linked against the prepared copy a Vosk symbol resolves and calls; linked against the file as shipped, dyld reports `Library not loaded: libvosk.dylib` and never consults the rpath |
| 50 | SEV-3 | Fixed | F-voice | The 39 MB Vosk model is committed as a ZIP and no CMake rule copies or extracts it, so it is not in the app bundle. TM-011 forbids writing a disk cache at run time, so extraction must happen at BUILD time | Adversarial PR review | Fix in F8b | extract into the bundle's Resources at build time | Closed in F8b/F8d: the model is extracted at BUILD time by `cmake/VendorVosk.cmake` and staged into `Contents/Resources/vosk-model`. Verified present in the built bundle; `make-test-build.sh` fails if it is missing or lacks the dynamic graph |
| 51 | SEV-3 | Fixed | F8a | GROUP AF has **zero production callers** — `audio_format.*` is compiled into `pptv_core` and called by nothing. 10 green tests protecting code no capture path reaches, and the defect class they exist for (feeding Vosk 48 kHz) is still fully live until F8b wires it up. Not wrong, but the green tests overstate what is actually protected | Adversarial PR review | Fix in F8b | wire it into the capture callback | Closed in F8d: `VoicePipeline::onSamples` calls `toRecognizerFormat` on every capture callback, so GROUP AF now protects a live path |
| 52 | SEV-2 | Fixed | F1a/F1b | **My BUG-47/48 fixes themselves contained UNDEFINED BEHAVIOUR** — both found under UBSan by a reviewer attacking the fix. (a) `static_cast<int>(pct * 1000.0)` in the new percentage parser: a float-to-int conversion whose value does not fit is UB, not a wrap, and `"1e30%"` is two characters away from any legitimate deck. (b) `sr.leftPerMille + sr.rightPerMille >= 100000` in the new over-crop guard: two large ints straight out of the deck overflow signed int INSIDE the check whose whole job is to reject them | Adversarial PR review round 2 | Fix Now | range-check before the narrowing cast; sum the insets in `qint64` | Verified: removing either guard makes UBSan fire on the new fixture (`1e+33 is outside the range of representable values of type 'int'`) |
| 53 | SEV-2 | Fixed | F1b | **`<a:stretch>` was never honoured: we letterboxed where PowerPoint STRETCHES.** Measured by a reviewer across the real deck — every picture drawn at the wrong aspect, so the crop could be exactly right and the picture still the wrong size and shape. Confirmed decisively from the deck: **all 51 `blipFill` elements carry `<a:stretch>`** (39 with fillRect, 12 with srcRect, 0 tile). BUG-10's own entry had said stretching "matches PowerPoint's fill default" and was deferred; aspect preservation was added later anyway | Adversarial PR review F1 | Fix Now | `<a:stretch>` read as a direct child of `blipFill` sets `stretchToFill`; the renderer fills the frame when set and keeps BUG-10's aspect-preserving fit when absent — a flag, not a replacement, so BUG-10's test (whose fixture has no `<a:stretch>`) still passes unchanged | Verified on the real deck: 5/5 pictures now stretchToFill. Reverting the fix turns the new test red |
| 54 | SEV-3 | Open | F1a | A NEGATIVE `<a:srcRect>` inset means PAD (an outset border), not crop. We clamp it away. The real deck has them: `(-6726,0,-1,36909)`, `(0,0,-6769,41679)`, `(0,0,-796,39415)` | Adversarial PR review | Defer | pad rather than clamp | — |
| 55 | SEV-3 | Open | F1a | **BUG-41 fixed only ONE of three paths** to a missing picture, and the reviewer says the cause was misdiagnosed. The committed fixture is not the shape the real deck has | Adversarial PR review | Fix in F7c | re-derive from the reviewer's evidence before changing code | — |
| 56 | SEV-2 | Fixed | F8a | The audio API **could not express buffer length** — it took a pointer plus a frame count and trusted them, so a device changing sample rate or channel count mid-stream (both events the F8a audit itself predicted) produced a **heap over-read** | Adversarial PR review | Fix Now | `toRecognizerFormat` now takes the samples ACTUALLY AVAILABLE and refuses when the format's implied length exceeds it; `sampleCountFor()` rejects an overflowing multiply | **Verified under ASan**: removing the check produces a real `heap-buffer-overflow` in `downmixToMono` |
| 57 | SEV-2 | Fixed | F1b | **A legitimate crop silently DELETED pictures.** The "cropped to nothing" guard was `< 1` measured in SOURCE pixels, so an ordinary 60x2 accent bar cropped `b=60000` (0.8 source pixels high) vanished — no warning, no placeholder. **BUG-41's defect class, reintroduced one commit later, by me.** The real deck uses `b=36909` | Adversarial PR review F3 | Fix Now | only a TRULY empty source is nothing to draw; a sub-pixel source rounds outward to one pixel, clamped to the image | Verified: restoring the `< 1` guard turns the new test red |
| 58 | SEV-3 | Fixed | F1a | A layout `<p:pic>` placeholder could **steal a text shape's geometry** — both collided on one key and the LAST in document order won, which in a layout is z-order, not priority. Not triggered on Karl's deck (all 45 layout placeholders sit under `nvSpPr`) but real | Adversarial PR review F4 | Fix Now | the key is namespaced by the non-visual holder that carried the `<p:ph>` | **Superseded — that fix was WRONG and broke the real deck (BUG-70).** The collision is now resolved at insertion (first entry wins) instead of by namespacing the key. 211 tests green |
| 59 | SEV-3 | Fixed | F1a | **BUG-41 dropped a picture on SLIDE 9 as well as slide 1** — the review found a second slide-level `<p:pic>` placeholder (`type='pic' idx='10'`). My BUG-41 entry and commit message mention only slide 1 | Adversarial PR review F4 | Note | already fixed by BUG-41; the record was incomplete, not the code | Record correction, not a code change — BUG-41's fix already covered slide 9. Verified: slide 9's picture loads with `crop l=31593 t=-190 r=40879 b=190`, 141880 bytes |
| 60 | SEV-1 | Fixed | F7 | **The suite cannot detect BUG-18's return.** Three mutations — delete the platform load `QShortcut`, stop `dropEvent` emitting, delete both `connect(start_, ...)` lines — each leave all 224 tests green and together restore the exact pre-BUG-18 state (deck openable only from argv). `S/BUG-18` asserts the button EMITS; nothing asserts anything is listening. `src/ui/app_shell.cpp`, every wire in the app, has zero tests | UAT-4 automated-suite | Fix Now | **All three BUG-18 mutations now killed.** `AppShell` gained a deck-load-attempt signal (see `app_shell.hpp`) and two narrow test seams; tests assert the wires exist, the drop handler emits, and the platform load shortcut is installed |
| 61 | SEV-2 | Fixed | F1b | **JPEG decode is untested; deleting it leaves the suite green.** All 36 image fixtures are PNG. Measured: with JPEG removed from `isAllowedImageFormat`, a JPEG renders as the grey missing-image placeholder and no test notices. **The reference deck contains 2 JPEG media parts** | UAT-4 automated-suite | Fix Now | JPEG fixture added; mutation m07 (JPEG removed from the allow-list) now killed |
| 62 | SEV-2 | Fixed | F1b | **The srcRect out-of-bounds clamps have zero coverage.** Dropping `.intersected(whole)` or the `qMin` 1-pixel clamps both survive the suite and both change rendered output. **6 of the reference deck's 12 `<a:srcRect>` elements carry negative insets** (`r="-6769"`, `l="-6726" r="-1"`, `t="-190"`) | UAT-4 automated-suite | Fix Now | `slideSourceRect()` exposed and asserted directly — a colour-based test was too coarse and m10 survived it; m10 now killed |
| 63 | SEV-2 | Fixed | F8a | **`downmixToMono` arithmetic is unconstrained.** `sum / channels` -> `sum / 2` survives: every downmix test uses 2 channels. A 3-channel buffer at 24000/channel gives 24000 pristine vs **-29536** mutated — a full-scale polarity flip, the exact click the adjacent overflow test claims to prevent. **A MacBook Pro's built-in microphone is a three-element array** | UAT-4 automated-suite | Fix Now | 1/3/4/8-channel downmix cases + exact resampled-count assertions; m01 and m02 now killed |
| 64 | SEV-2 | Fixed | F7 | The three surfaces no automated test could verify. **(a) projector — Karl, 2026-08-05**, first hardware confirmation BUG-25's fix works. **(b) voice recognition — Karl, 2026-08-06**, all five commands on the MacBook Pro, first run against a human voice. **(c) false triggers — Karl, 2026-08-06**: background video of people talking at ~25% below his voice, commands still worked, **no inadvertent slide changes**. That is TM-002/TM-019 verified empirically rather than only by construction | UAT-4/5 | Closed | — | All three verified by the human arm on the target hardware |
| 65 | SEV-2 | Fixed | F8c | **Vosk silently DROPS grammar tokens outside the model vocabulary**, quietly widening what can be recognised — a typo in the grammar would not fail, it would just admit more words. Detectable only against a loaded model, so out of scope for the grammar-preconditions slice | F8c security audit | Fix in the decode slice | after creating the recogniser, verify every grammar word round-trips through the model's vocabulary lookup and refuse to run if any is unknown | Closed in F8d: every grammar word is round-tripped through `vosk_model_find_word()` before the recogniser is created, and any unknown word aborts with GrammarRejected. Verified against the real vendored model, which passes |
| 66 | SEV-1 | Fixed | F8c | **The decoder is NOT "incapable of emitting anything but the five commands" — my CHANGELOG and header claim is FALSE.** Vosk's grammar mode compiles the word list into a **backoff bigram LM** (`ngram_order>=2`), not a phrase FSA, so it emits cross-phrase sequences. 12 min of babble produced 51 results, 28 distinct strings, none a command — e.g. `"eight resume"`, `"eleven zero eighty slide"`. What DOES hold is the LEXICAL constraint: across 404 utterances, 39 distinct words, **zero outside the grammar vocabulary**. Correct claim: "closed vocabulary, open word order" | UAT-5 grammar-escape | **Fix Now** | withdraw the claim in CHANGELOG.md and vosk_recognizer.hpp; state the real property | CHANGELOG and header now state the real property: closed vocabulary, open word order. Measured: every word emitted across the corpus was in-grammar |
| 67 | SEV-1 | Fixed | F8c | **No `[unk]` token, so ordinary English is FORCE-FIT onto commands.** With no legal path for out-of-grammar audio the decoder must snap every utterance to the nearest command. **98 of 102 near-miss utterances fired a real command**: `next flight`/`next light`/`annex slide`/`obvious slide` -> NextSlide/PreviousSlide; `applause presentation`/`pause president` -> PausePresentation; **`continue presenting`/`presume the presentation` -> ContinuePresentation, which UN-PAUSES the deck during the Q&A window the Paused state exists to protect**. Adding `[unk]` naively is BOOBY-TRAPPED: `grammarJson()` allows only `[a-z ]`, so `[unk]` becomes `unk`, which Vosk drops as OOV — and BUG-65's guard misses it because it inspects the phrases, not the sanitised JSON. Measured: adding `[unk]` drops hits 98 -> 85, so a grammar-only defence CANNOT deliver the claimed property | UAT-5 grammar-escape | **Fix Now** | allow `[unk]` through the JSON builder, require it, and validate it survives; then re-measure | `[unk]` present and validated in the emitted JSON. **Measured on 102 clips x 3 voices: 75/102 -> 60/102 false triggers, true-command recognition unchanged at 13/15.** A ~20% reduction, not elimination. All six NATURAL SENTENCES fired zero times; every residual is an isolated near-miss fragment. **Follow-on defect: BUG-71** — the `[unk]` token also appears beside legitimate commands, and rejecting those broke 36/60 natural phrasings |
| 68 | SEV-1 | Fixed | F8c | **Every force-fit reports `conf: 1.000000`.** A genuine "next slide" and someone saying "next flight" are bit-identical in confidence: with a closed grammar and no `[unk]` there is no competing hypothesis, so the posterior is 1.0 by construction. **A confidence threshold cannot be retrofitted until `[unk]` exists** — this forecloses the cheapest-looking mitigation | UAT-5 grammar-escape | Fix with BUG-67 | `[unk]` first, then a threshold becomes meaningful | `[unk]` now exists, so a competing hypothesis exists and confidence is no longer 1.0 by construction. A threshold is now POSSIBLE; not implemented, and not needed given the measured rate |
| 69 | SEV-2 | Fixed | F4/F8c | Ordinary conference prose produced `"go to slide zero two three go six"` -> **GoToSlide(236)**. The bigram sprays `go`/`to`/`slide`/number words; `matchCommand` sees the prefix; `parseSlideNumber` treats the stray `go` as filler | UAT-5 grammar-escape | Fix Now | reject a jump whose number contains non-number filler words | `go to slide zero two three go six` -> no command; `go to slide one hundred and twenty` -> 120. Filler mid-number aborts the jump, with `and` the sole exception |
| 70 | SEV-2 | Fixed | F1a | **Phase 3 finding C-01 — my own BUG-58 fix reopened BUG-41 on the real deck.** BUG-58 namespaced the placeholder key by the non-visual-properties element carrying the `<p:ph>`. The reference deck's slide-1 photograph sits under `<p:nvPicPr>` while the layout entry positioning it sits under `<p:sp>`, so the two stopped matching: the picture inherited no geometry, returned to `0x0`, and was silently never drawn — **no warning**. BUG-58's commit claimed "not triggered on Karl's deck (all 45 layout placeholders sit under `nvSpPr`)"; that verified the LAYOUT side only, and a key must match on both sides | Phase 3 validation (integration validator + independent skeptic bisect) | Fix Now | placeholder identity is `type:idx`, which is what ECMA-376 defines and PowerPoint writes; the pic/sp key collision BUG-58 was about is handled at INSERTION in `parseLayoutPlaceholders` (first entry wins) so document order — z-order, not priority — no longer decides whose geometry a slide adopts | Verified on the real deck: slide 1 element 1 is `IMAGE rect=(8017727,-1) 4282068x6858001 bytes=198219 crop l=29178 r=29178`, 0 warnings across 10 slides. Regression test asserts a `<p:pic>` placeholder matches a layout entry under a DIFFERENT holder |
| 71 | SEV-2 | Fixed | F8c | **Phase 3 finding C-02 — my own BUG-67 fix broke natural phrasing.** Vosk emits a literal `[unk]` token wherever it heard something the grammar cannot express, so any real command with adjacent speech arrives as `[unk] next slide` or `next slide [unk]`. Measured by an independent skeptic across a 60-clip corpus of natural phrasings: **36 of 60 rejected** — "okay next slide", "next slide please". The obvious fix is booby-trapped: the same skeptic produced the counterexample that kills it — the prose sentence "the next slide shows our results for this quarter" decodes as `[unk] next slide [unk]`, so treating `[unk]` as ordinary strippable filler turns NARRATION into a slide change and reopens TM-002/019 | Phase 3 validation (independent skeptic) | Fix Now | strip `[unk]` at exactly ONE edge, never both; `[unk]` on both sides means speech on both sides of the command words, i.e. a sentence containing them rather than someone issuing one | Verified both directions: `[unk] next slide`, `next slide [unk]`, `[unk] pause the presentation`, `go to slide seven [unk]` all fire; `[unk] next slide [unk]` and `the next slide shows our results` fire nothing |
| 72 | SEV-2 | Fixed | F7/F-voice | **Phase 3 finding F-1 — `~AppShell` freed `VoskEngine` while the audio thread was inside `feed()`.** The destructor called only `teardownWorkers()`, which touches the load and render threads; voice was never stopped. Members are then destroyed in reverse declaration order, and `engine_` was declared after `voice_`, so the engine went first while CoreAudio's real-time thread was still calling into it. **Invisible on the development Mac mini — it has no input device — and live on the presenter's MacBook Pro, on every Cmd+Q at the end of a talk** | Phase 3 validation (integration validator; reproduced 7/7 under AddressSanitizer by the reviewer) | Fix Now | `teardownVoice()` at the TOP of `~AppShell`: stop the pipeline (which joins the audio thread), then free the engine, then the gate. Declaration order reversed as a second, independent mechanism | **Reproduced and fixed, measured**: a harness replicating the ownership shape gives `heap-use-after-free` in **7/7** runs in the old order and **0/7** sanitizer errors in the new one, with both stacks matching the reviewer's (`Engine::feed` reading a block freed by `unique_ptr::reset` on the GUI thread). **Two corrections from the adversarial review of this fix.** (1) The mechanism was misstated — `ma_device_stop()` provides no such join; the real barrier is `sinkMutex_`, and it was on the wrong side of the device teardown (BUG-79). (2) The regression test was worthless: it pinned a property of a fake capture that joins BY CONSTRUCTION, and the reviewer deleted this entire fix — the `teardownVoice()` call AND the declaration order — with all 279 tests still green. `W/F-1` now counts VoskEngine destructions from inside a live decode and fails 3/3 under that same full revert |
| 73 | SEV-2 | Fixed | F7 | **Phase 3 finding F-2 — the P key was DEAD, and 267 green tests said nothing.** `PresentationWindow::setPaused()` had **zero callers**, so `KeyContext::paused` was permanently false, P always translated to `PausePresentation`, and `PresentationController` treats pause and continue as no-ops. Pressing P did nothing at all — while looking, to the presenter, exactly like the pause they were relying on before taking questions from a room of twenty people | Phase 3 validation | Fix Now | `RecognizerController::setPaused()` (the gate is the single owner of Paused); the keyboard sink routes pause/continue through it and keeps the window's flag in step; the voice sink does the same after a SPOKEN pause, so P stays a true toggle either way. With voice not armed P is inert rather than reporting "Resumed" about a subsystem that is not running | Three mutations killed: inert `setPaused`, removed keyboard routing, and the window flag. Seven new tests across the gate and the widget layer |
| 74 | SEV-2 | Fixed | F7 | **Phase 3 finding F-CHAOS-2 — an abandoned render worker painted the PREVIOUS deck onto the projector.** `teardownWorkers()` deliberately ABANDONS a worker whose wait expires rather than terminating it (BUG-42: terminate can strand allocator locks, and destroying a running QThread is a qFatal abort). The abandoned worker keeps rasterising the old deck, and its `slideReady` was still connected — so old slides landed in `rasters_` after `openDeck()` had resized it for the new deck. Every index is in range, so nothing complained | Phase 3 validation (chaos) | Fix Now | two independent guards: `disconnect(worker, nullptr, this, nullptr)` in `teardownWorkers()` cuts the wire, and a **deck generation counter** checked in `acceptSlide()` drops anything Qt had already queued. The load worker carries the same generation check, so a stale deck cannot be delivered either | Mutation killed: removing the generation check turns the new test red |
| 75 | SEV-2 | Fixed | F7 | **Phase 3 finding F-CHAOS-3 — no exception boundary on either worker thread.** Both `start()` methods are slots invoked on a QThread, and Qt does not catch: an exception leaving one unwinds through the worker's event loop and `std::terminate()`s the process — no dialog, no fallback, the talk over. Both do work sized by an UNTRUSTED file, so `std::bad_alloc` is a normal outcome of a normal input | Phase 3 validation (chaos) | Fix Now | loader: `catch(...)` around the parse, reported as the closed-vocabulary "The deck could not be opened." (no exception text ever reaches a dialog — it can carry a path or file bytes, TM-013). Renderer: a boundary **per slide**, so one unrenderable slide costs one placeholder box rather than the remaining ninety-nine, plus a backstop around the loop and around the placeholder fallback itself | Mutations killed: removing the loader boundary makes the test die with SIGABRT — the exact `std::terminate` the fix prevents; removing the renderer's makes the null-raster floor test red |
| 76 | SEV-2 | Fixed | Test harness | **Tests that had NEVER ONCE RUN, while the suite reported 100% green — 5 of them shipped that way, and a 6th was committed dead the same day.** `doctest_discover_tests()` registers each TEST_CASE name into a CMake list, and CMake's list separator is `;` — so a test whose NAME contains a semicolon is registered TRUNCATED, ctest invokes `--test-case=<fragment>`, doctest matches nothing, runs 0 cases and exits 0, and ctest prints **Passed**. Affected: `A: one past the end is rejected…`, `D: Esc goes to the holding screen…`, `D: a second Esc asks to quit…`, `BUG-53: a stretched picture fills its frame…`, `UAT2 BUG-11/17: 'resume presentation' un-pauses…`, `R/F-CHAOS-3: a throwing slide…`, and **`C-02: an [unk] on ONE edge is stripped…` — the BUG-71 regression test committed the same day**. **Counted precisely** (`git grep 'TEST_CASE(".*;'`): **5 at the branch point**, dead since Phase 2 and UAT remediation; **6 after the C-02 commit** hours earlier; **7 in the working tree at the moment of discovery**, the 7th caught before it was committed. The only visible symptom was ctest listing 282 tests where the binaries contain 275 | Found while mutation-testing the F-CHAOS-3 fix: the mutation failed the test when run directly and "passed" under ctest | Fix Now | all seven renamed; `scripts/lint-test-names.sh` compares the ctest registration against `--list-test-cases` in BOTH directions and fails on any mismatch, wired into `scripts/run-tests.sh` (the commit-time gate) and CI | Verified by reintroducing a `;` into an unrelated test name: the lint fails, naming both the unreachable case and the phantom registration. All 275 cases now run; the seven recovered tests pass |
| 77 | SEV-2 | Fixed | F7 | **Phase 3 finding A11Y-1 — the presentation was SILENT to VoiceOver.** Every surface is a custom-painted `QWidget` with no accessible name, no description and no text for the platform to infer, so a screen reader sees one unnamed rectangle: the deck, the privacy blackout, the quit prompt and a hung app are indistinguishable. The window has no menu bar and no on-screen control either, so there was nothing to discover the key map from | Phase 3 validation (accessibility) | Fix Now | the window carries the key map as its accessible description; the surface announces WHICH slide (`"Slide 3 of 10."`), the blackout and the quit prompt; the notice strip raises `QAccessible::Alert` so a transient notice is spoken rather than left as a property nobody asks for. **The accessibility tree is readable by other processes and the deck is Confidential, so the STATE is named and the CONTENT never is** (Bible section 8, TM-012/013) | 4 tests, including one asserting the shell actually WIRES the state (BUG-60's lesson: naming is worthless if nothing updates it). Mutation killed: deleting the `setAccessibleState` call in `showSlide` turns it red |
| 78 | SEV-2 | Fixed | F7 | **Phase 3 finding A11Y-2 — the pause gate was unreachable from the keyboard.** Voice was the only way to reach Paused, so a presenter who could not or would not use the microphone had no way to gate it at all | Phase 3 validation (accessibility) | Fix Now | closed by the BUG-73 fix: P reaches the same gate the voice does, in both directions | Same tests as BUG-73 |
| 79 | SEV-2 | Fixed | F-voice | **The BUG-72 fix rested on a guarantee the audio library does not provide, and the real barrier was on the wrong side of the teardown.** `teardownVoice()` and two headers asserted "`ma_device_stop()` does not return until the audio callback has finished". The vendored miniaudio 0.11.25 does not implement that on CoreAudio, and the whole chain is in the file we ship: `ma_device_stop__coreaudio` (36443) waits on `coreaudio.stopEvent`, which is signalled at exactly one place — the `done:` label of `on_start_stop__coreaudio` (35367), a listener on a property that changes on **START as well as stop**; `ma_event` is latching (`ma_event_wait__posix`, 17768, returns immediately when `value != 0`), so the wait consumes a stale signal left by device start and waits for nothing. It is a property-change notification in any case, not the data callback: `ma_on_input__coreaudio` (35171) checks no device state and takes no lock. `ma_device_uninit` then frees `pAudioBufferList` and `MA_ZERO_OBJECT`s the device while a callback may still hold a pointer into it — and deliberately **no longer stops the device first** (that block is `#if 0`, 44119). The code was in fact safe, but by an undocumented mechanism — `sinkMutex_`, which `onData` holds across the whole `sink_()` call — **taken AFTER `ma_device_uninit()`**. Separately, `VoicePipeline`'s own member order was backwards: `capture_` declared first meant destroyed LAST, so `running_` and `decode_` were torn down while the capture thread could still be inside `onSamples` | Adversarial PR review of the BUG-72 fix | Fix Now | `MiniaudioCapture::stop()` clears the sink under `sinkMutex_` **before** `ma_device_stop`/`ma_device_uninit`, so the join happens before the device is touched; `capture_` declared LAST in `VoicePipeline` so it is destroyed FIRST; every comment restated against what miniaudio actually does | Every claim in the evidence chain re-verified independently against `third_party/miniaudio/miniaudio.h` before the fix was written |
| 80 | SEV-2 | Fixed | F7 | **The BUG-77 accessibility fix was a NO-OP on the only platform this ships on.** It raised `QAccessible::Alert` and `QAccessible::DescriptionChanged`. Neither has an AppKit equivalent, so Qt's Cocoa plugin discards both and VoiceOver says nothing. `nm -mu libqcocoa.dylib` gives the complete set the plugin can post: Focused / SelectedText / Title / ValueChanged notifications, plus `NSAccessibilityAnnouncementRequestedNotification` with `AnnouncementKey` and `PriorityKey`. Only the last carries a message. The four A11Y tests asserted the accessible DESCRIPTION strings, which the no-op version set perfectly well — so they passed while nothing was ever spoken | Adversarial PR review of the BUG-77 fix (disassembled the linked Cocoa plugin) | Fix Now | `QAccessibleAnnouncementEvent`, which is the one event type that reaches the platform | Two new tests capture the events via `QAccessible::installUpdateHandler` and assert `Announcement` is raised and `Alert` is not — the events, not the strings |
| 81 | SEV-1 | Fixed | F7 | **Holding P left the microphone LIVE — the BUG-73 fix re-created BUG-73's own failure mode.** P is a toggle and nothing checked `isAutoRepeat()`. macOS repeats a held key after ~500 ms at ~11/s, so holding P for a second gives one press plus ~5 repeats; six flips lands back on **Active**. The presenter believes voice is gated, turns to twenty people for questions, and the room can drive the deck. Measured by a reviewer against the real sink: *"after ONE held press of P the gate is ACTIVE (voice still live!)"*. Esc has the same shape — held, it walks Presenting → Holding → ConfirmQuit with no second deliberate press | Adversarial PR review of the BUG-73 fix | Fix Now | `KeyContext::autoRepeat`, and `KeyCommandTranslator` swallows an auto-repeated P or Esc. Navigation and digits stay repeatable — holding an arrow to run forward is a real thing presenters do | 4 tests, in the pure translator so they need no window |
| 82 | SEV-2 | Fixed | F7 | **A pause that WORKED was indistinguishable from a pause that did nothing.** `PausePresentation` produced no notice, so a successful pause, the dead P key of BUG-73, and the early return when voice is not armed all looked identical: an empty strip. Resuming *did* show "Resumed". The presenter had no way to confirm the microphone was gated before taking questions — and no way to see BUG-81 putting them straight back to live. `NoticeId::Paused` ("Paused — voice control is off") already existed and was **structurally unreachable**: nothing emitted it, and `noticeForRole` suppresses it unless the caller passes `paused=true`, which `AppShell` hardcoded to `false` | Adversarial PR review of the BUG-73 fix | Fix Now | `dispatch()` emits `NoticeId::Paused` as a **Sticky** notice, and `applyResult` passes the gate's real state instead of a literal `false` | 2 tests; the notice is asserted both present and rendered |
| 83 | SEV-2 | Fixed | F-voice | **The real-time audio thread had no exception boundary — BUG-75 covered the two worker threads and missed the one that runs during the whole talk.** `VoicePipeline::onSamples` executes inside miniaudio's C data callback, and every buffer allocates: a `std::vector` for the downmix, then a `std::string`, `QByteArray`, `QJsonDocument` and `QString` inside the decoder, then a `QMetaCallEvent` for the queued hand-off. An exception unwinding out of a C-ABI frame is undefined behaviour and in practice `std::terminate` — the process dies mid-sentence with the deck on the projector | Adversarial PR review of the BUG-75 fix | Fix Now | `catch(...)` in `onSamples`, and a second at the C boundary in `MiniaudioCapture::onData`. A dropped buffer is 20 ms nobody notices; nothing is logged, because what this callback carries is everything said in the room (TM-012/013) | 3 tests, including one asserting the NEXT buffer still decodes — a transient failure must not kill voice for the rest of the talk |
| 84 | SEV-2 | Fixed | Test harness | **The BUG-76 lint had three holes, all demonstrated by a reviewer with a deliberately-broken probe project.** (a) **FAIL-OPEN:** it found binaries with `find -name '*tests'` and exited 0 with a reassuring message when that matched nothing — a target named `pptv_chaos_test` silently disabled the whole check, CI step included. (b) **FALSE NEGATIVE on newline:** `doctestAddTests.cmake` splits on `\n` as well as `;`, and the lint compared name SETS read line-by-line, applying the identical split to its own side; the two agreed and it reported green on a genuinely dead test. So its header claim to catch "any future truncation of any kind" was false. (c) **FALSE POSITIVE:** any ordinary non-doctest `add_test` was reported as "matches NO test case" and blocked the build | Adversarial PR review of the BUG-76 fix | Fix Now | binaries are discovered from **ctest's own command lines** (no name convention to get wrong, and finding none is an ERROR); the authority is now a **count** — doctest's own `unskipped test cases passing the current filters: N` against the number of ctest entries invoking that binary — which no separator can disguise; only entries actually passing `--test-case=` are counted | All three re-verified: a newline in a test name is now caught (261 vs 262), a plain `add_test` no longer trips it, and the fail-open path is gone by construction |
| 85 | SEV-3 | Fixed | Test harness | **Two of the BUG-73 widget tests wired the window to a DIFFERENT `PresentationController` than the shell's, making one assertion unfalsifiable.** `CHECK(c.currentSlide1Based() == 1); // and it certainly did not move the deck` could not fail: `c` was the window's controller and the sink dispatches into `AppShell::controller_`. A reviewer showed three right-arrows leaving `c` at 1 while the shell had advanced to 4 | Adversarial PR review | Fix Now | `controllerForTest()` exposes the controller the sinks actually write to; both tests build their window on it, and one now asserts a right-arrow DOES move it, so the check has teeth | — |
| 86 | SEV-3 | Fixed | F-voice | **`openDeck()` tore down the workers but never voice, and `armVoice()` is not idempotent** — it assigns `engine_` before creating the new pipeline, so re-arming over a live one frees the engine the audio thread is inside. BUG-72 exactly, inverted, on the path the BUG-72 fix did not touch. Not reachable today (the start screen is hidden after the first successful open, and Cmd+O is window-scoped to it), but `openDeck` and `browseForDeck` are both public and "open another deck" is one feature away | Adversarial PR review | Fix Now | `teardownVoice()` at the top of `openDeck()` | Latent, closed before it could be rediscovered from a crash report |
| 87 | SEV-2 | Fixed | F7 | **The BUG-80 fix did not COMPILE on Linux — CI caught what three adversarial reviewers and a green macOS build did not.** `QAccessibleAnnouncementEvent` and `QAccessible::Announcement` both arrived in **Qt 6.8**. The dev and release platform is macOS with Qt 6.11, so the fix and all its tests were green locally; CI builds against Ubuntu's packaged Qt6, which is older, and the build failed outright in `slide_surface.cpp` and `notice_strip.cpp`. A useful reminder that "verified" on one platform is verified on one platform | CI, on the PR that carried the BUG-80 fix | Fix Now | `src/ui/a11y_announce.{hpp,cpp}` — one shared, version-guarded helper rather than a guard duplicated at two call sites, which is a guard that drifts. Below Qt 6.8 it is a deliberate **no-op**: the alternatives there are events the macOS bridge discards, and raising one would put the "we announce" claim back into the code while changing nothing anyone can hear, which is exactly what BUG-80 was. Accessible names and descriptions are still set, so navigating to an element still reads it. `accessibilityAnnouncementsAvailable()` lets the tests assert against the build's real capability instead of assuming **And the first attempt at this fix ALSO failed CI**, for the same reason one level over: `QAccessible::Announcement` is an ENUMERATOR, so the tests naming it failed to BUILD however carefully they checked at run time. My old-Qt simulation had forced the guard in `a11y_announce.cpp` only — not where the difference actually bites. **Simulate at the boundary that differs, not at the one you happened to write.** Both paths now verified across src AND tests: Qt 6.11 announces and its tests assert on the EVENT; with every guard forced off the tree compiles clean and 290 tests pass, the 2 announcement tests compiled out |
